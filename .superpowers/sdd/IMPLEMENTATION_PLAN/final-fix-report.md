# Final whole-branch fix report

**Status: DONE_WITH_CONCERNS — source complete; successor package/re-review and native acceptance are separate.**

Agent: `/root/final_fix`. Date: 2026-09-07. Repository: `C:/p/evidra-zotero`, branch `codex/implement-evidra`. Reviewed base: `08d3cf6e5f0c2456ef9a4ea9842e8c768c9add51`. Implemented source head: `8c7739b39eafdffb0f6991c4a66738c06023e9ec`. This report and the historical STATE reconciliation are the final documentation increment after that source head.

Read the entire `whole-branch-review.md` and the binding final-fix brief, Global Constraints/Integration contracts and relevant SPEC source/provenance/extraction/export requirements. Used Superpowers debugging, bounded design reasoning, proportional TDD and verification. No children, model calls, native application operations, package/build/benchmark runs or remote writes occurred. Existing migrations001–014 and dependency manifests are unchanged.

## Findings disposition

| Finding | Disposition and evidence |
| --- | --- |
| I1 admitted textual files | **Addressed in source and fixtures.** Native MIME provenance, private verified-file registration, isolated bounded text/HTML/XML/EPUB parser, immutable byte hash/original extracted text, FTS evidence and full-scan planning are connected. Unsupported/invalid formats and limits remain explicit failures. Unpaginated evidence has text precision and no fabricated PDF page/geometry. Verified native text/EPUB location opening remains unavailable and is explicitly labeled. |
| I2 snapshot revision conflation | **Addressed.** Local CSV selects only its selected snapshot; revisions stay within snapshot and field lineage. Source records retain each snapshot association. Both orientations include `snapshot_id`/`source_version`; imported history retains separate original snapshot/group rows. Portable JSON/backup retain history. The exact original package3 backup remains readable/projectable. |
| M1 document reason localization | **Addressed.** Documents reuses existing coverage labels and adds file-change/text-parser explanations in both locales, retaining machine codes in parentheses. |
| M2 operation kind | **Addressed.** Documents names indexing or preview before state/counters using existing labels; parser coverage remains separate. |
| M3 failing keyring test | **Addressed.** The existing failed-keyring store now uses `services.database`; the test verifies the persisted metadata receipt, memory-only secret, backup exclusion and clear behavior. No credential leak was established or claimed fixed. |
| M4 current/historical state | **Addressed.** STATE has one current source/package checkpoint; all superseded blocks are labeled historical. Package3 is precisely bound to08d, and this later source wave is explicitly absent from it. The prior Portuguese decoding mistake is identified without altering historical text or source quotations. |
| M5 warning/capture debt | **Deferred as instructed.** React act and Starlette/AnyIO warnings and RTK hook/capture debt remain visible. No warning suppression, hooks, dependency maintenance or broad retesting was added. |

## Changes and limits

`SourceContent.media_type` is optional in the generated contract but required for new textual ingestion. Native resolution records the actual MIME; revalidation checks it against the current item. Missing MIME on an old text snapshot produces `SCOPE_STALE` and requires recapture. Excluding an absent MIME field from content hashing preserves existing immutable identities; it is not a parser fallback. No filename-based MIME guess or renderer path/file API was added.

Text files reuse the existing regular-file/reparse/identity checks, streamed SHA-256, current server-owned scopes, Windows Job subprocess limits, cancellation and atomic version publication. PDF parsing/preview stays on its original path. File observation, lexical/vector eligibility and portable collection now apply file-identity checks to text attachments too. Failed parser reasons propagate into full-scan coverage instead of becoming a generic `NOT_INDEXED`.

The implemented parsers support plain/CSV/TSV/Markdown, HTML, XML/XHTML and EPUB with one rendition and XHTML spine resources. Plain decoded text retains exact Unicode/line endings; markup preserves extracted ordered text nodes with explicit block boundaries. EPUB consumes the complete declared spine in order. XML entities/DTDs, unsupported encodings/types, encrypted/multiple-rendition/unsupported-spine EPUBs and malformed archives fail explicitly. ZIP entries/decompression and text units are bounded; no extraction to disk, resource fetch or hidden parser substitution occurs. An EPUB unit without text fails with `INCOMPLETE_TEXT`. Text limits also bound the engine normalization input; cached text versions are reparsed to enforce the current request's limits. Detailed format, encoding, text-unit and native-navigation semantics: `docs/TEXT_ATTACHMENTS.md`.

The text unit is unpaginated: physical page/label/rectangles are null/empty and offsets refer to the stored original extraction. Native Zotero10.0.1 source inspection shows EPUB Reader locations use CFI, while `_getData()` rereads the current file; neither is the existing PDF loaded-proxy byte/geometry contract. No verified loaded-byte/CFI mapping was established. The UI therefore displays the verified excerpt and a localized instruction to inspect the original attachment in Zotero; it never sends textual files through PDF Reader/preview. This is an explicit remaining citation-navigation limitation, not native acceptance.

For CSV, original portable source records had been deduplicated by source/version, losing repeated snapshot associations. The producer now emits an independently identified source record per snapshot while preserving immutable source data/version. Local projection uses the selected snapshot; imported records remain historical rows by original snapshot/group. An older deduplicated portable source is usable when one original version unambiguously identifies it; otherwise projection fails with `CSV_SOURCE_VERSION_AMBIGUOUS`. No source version is inferred from a higher unrelated cell revision, and no archive history is deleted. Form field-origin filtering remains intact.

References inspected: [Python3.12 XML](https://docs.python.org/3.12/library/xml.html), [ZIP](https://docs.python.org/3.12/library/zipfile.html), [HTMLParser](https://docs.python.org/3.12/library/html.parser.html), [Zotero10.0.1 Reader](https://github.com/zotero/zotero/blob/10.0.1/chrome/content/zotero/xpcom/reader.js). These informed local parser/API choices; no external operation is claimed.

## Verification receipts and original failures

All receipts below are under `C:/p/evidra-zotero/.local/final-fix/runs/`. Each includes the exact manifest/argv, exit status and full stdout/stderr paths. `C:/p/evidra-zotero/.local/final-fix/source-verification-binding.json` binds22 changed source/test/generated inputs to head8c7739b, lists the three source/test commits, and hashes all11 receipts. Checks are an aggregate of focused runs, **not a fresh all-green full suite**.

| Run directory | Result and causal interpretation |
| --- | --- |
| `csv-red-d3f724d7fe73451ca8797180f5376d6a` | 2 failed/1 passed. Initial test setup passed duplicate `title` kwargs to the existing source fixture; `TypeError` is preserved and is **not** the product RED. |
| `csv-red-5bd149c41ad64f4781b89ad8044a1519` | **Product RED:** 2 failed/1 passed. Both actual HTTP/SQLite CSV modes emitted42/revision2 when the selected snapshot approved99/revision1. |
| `csv-green-d186c4fcbcbe4139a3ccd5f7369b1ef5` | 2 failed/2 passed. Product assertions passed, but the added imported-history fixture used nonconforming import ID`import-1`; Pydantic rejected it. Corrected to a valid fixed32-hex ID, without weakening validation. |
| `csv-green-04ff7efae0634508b1c4275de16443ee` | **4 passed**, plus changed-file Ruff passed. Covers existing accepted-cell rejection, both new cross-snapshot orientations, imported historical rows/group/version separation, archive history retention, backup reference validation and existing actual roundtrip import. Starlette/AnyIO warning retained. |
| `text-red-197a97f0f2a54e0daeb051b62e33e723` |1 failed: original source schema rejected new native MIME provenance with422. This is the missing contract boundary, not the strongest ingestion RED. |
| `text-red-a535f54bcc524954b4b200d733e9f023` | **Product RED:**1 failed after MIME contract admission; actual file registration returned422 `UNSUPPORTED_DOCUMENT: Only authorized PDF files use file registration.` |
| `text-green-996737c9b1504fdca177cc1d71260650` |13 passed: initial12 parameterized actual text/file/worker/SQLite cases plus the existing PDF evidence case. Later explicit failure/cache-limit cases expanded the same regression. |
| `source-checks-5374a0e6ab1148039e52a3db27845333` | TypeScript passed;3 selected document UI/bridge cases passed (42 intentionally unselected, including locale cases whose titles did not match the initial filter); strict mypy passed10 changed source modules; corrected keyring test1 passed. UI/Starlette warnings retained. |
| `text-green-0bffc1e31dc4480f9ade48f17011dfe7` |15 passed/1 failed. All other final ingestion/PDF cases passed. The extra text-size fixture used64MiB, and Windows correctly reported `PARSER_MEMORY_LIMIT` with Job message9 before the expected text-size guard. This is preserved as a real enforced worker limit, not a swallowed exception. The fixture was changed to the default512MiB and2,100,000 characters to isolate `TEXT_LIMIT`; no product memory-limit weakening occurred. |
| `final-checks-c37cdd0cd384475bbf1177d57a5f38ee` | The corrected text-size case1 passed; existing locale key/UTF-8 parity1 passed (4 intentionally unselected); final changed-file Ruff passed; strict mypy passed the3 subsequently changed parser/ingestion modules. No repeated full suite. |
| `original-backup-2a372b95ca734c6e85249a69aa6e625f` | Exact original package3 backup passed current-source `read_backup`, `validate_references` and both CSV projections.44 records,1 embedded PDF;4 result rows/1 study row, all local rows on selected snapshot`f0912a3961ac87d6d497c7fc8504812b`. Original hash matched before/after. Zero native database/profile writes. |

Final distinct fixture counts: **16 ingestion/PDF cases,4 CSV/backup/import cases,1 keyring case,3 document UI/bridge cases,1 locale parity case**. The text regression is one parameterized function; bridge/UI cases extend existing tests. The original-backup check is an ignored one-off script, not a new permanent suite. No model calls or successful native ingestion/navigation are inferred from fixtures.

### Exact covering commands

Every shell invocation below uses RTK. Harness manifests in `.local/final-fix/{csv-red,csv-green,text-red,text-green,source-checks,final-checks,original-backup}.json` preserve the complete argument vectors and paths; each was run with:

`rtk proxy py -3 C:/Users/marcu/.codex/tools/workflow-harness/harness.py run C:/p/evidra-zotero/.local/final-fix/<manifest>.json --runs-dir C:/p/evidra-zotero/.local/final-fix/runs`

Python executable for every project check is `C:/p/evidra-zotero/services/engine/.venv/Scripts/python.exe` (project Python3.12 environment). The substantive commands were:

```text
rtk proxy C:/p/evidra-zotero/services/engine/.venv/Scripts/python.exe -m pytest services/engine/tests/test_exports.py::test_export_current_cell_survives_rejection_of_a_competing_proposal services/engine/tests/test_exports.py::test_versioned_roundtrip_has_typed_imported_history_without_local_authority -q
rtk proxy C:/p/evidra-zotero/services/engine/.venv/Scripts/python.exe -m pytest services/engine/tests/test_documents.py::test_text_attachment_ingestion_preserves_file_evidence_and_full_scan services/engine/tests/test_documents.py::test_registered_pdf_becomes_verifiable_scoped_evidence -q
rtk proxy C:/p/evidra-zotero/services/engine/.venv/Scripts/python.exe -m pytest services/engine/tests/test_documents.py::test_text_attachment_ingestion_preserves_file_evidence_and_full_scan[text_limit] -q
rtk proxy C:/p/evidra-zotero/services/engine/.venv/Scripts/python.exe -m pytest services/engine/tests/test_provider_security.py::test_memory_secret_and_failed_keyring_do_not_enter_sqlite_or_backup -q
rtk proxy npm.cmd run typecheck
rtk proxy npm.cmd test -- apps/zotero/tests/documents-ui.test.tsx apps/zotero/tests/documents-bridge.test.ts apps/zotero/tests/locale-accessibility.test.tsx -t "production document bridge|document panel|locale"
rtk proxy npm.cmd test -- apps/zotero/tests/locale-accessibility.test.tsx -t "complete catalogs"
rtk proxy C:/p/evidra-zotero/services/engine/.venv/Scripts/python.exe -m mypy --config-file services/engine/pyproject.toml services/engine/src/evidra/documents/text_parser.py services/engine/src/evidra/documents/ingestion.py services/engine/src/evidra/documents/parser_worker.py services/engine/src/evidra/documents/registry.py services/engine/src/evidra/domain/sources.py services/engine/src/evidra/exports/csv.py services/engine/src/evidra/exports/notebook.py services/engine/src/evidra/extraction/runner.py services/engine/src/evidra/retrieval/lexical.py services/engine/src/evidra/retrieval/vectors.py
rtk proxy C:/p/evidra-zotero/services/engine/.venv/Scripts/python.exe -m mypy --config-file services/engine/pyproject.toml services/engine/src/evidra/documents/text_parser.py services/engine/src/evidra/documents/parser_worker.py services/engine/src/evidra/documents/ingestion.py
rtk proxy C:/p/evidra-zotero/services/engine/.venv/Scripts/python.exe -m ruff check services/engine/src/evidra/documents services/engine/src/evidra/domain/sources.py services/engine/src/evidra/extraction/runner.py services/engine/src/evidra/retrieval/lexical.py services/engine/src/evidra/retrieval/vectors.py services/engine/src/evidra/exports/csv.py services/engine/src/evidra/exports/notebook.py services/engine/tests/test_documents.py services/engine/tests/test_exports.py services/engine/tests/test_provider_security.py
rtk proxy C:/p/evidra-zotero/services/engine/.venv/Scripts/python.exe -B C:/p/evidra-zotero/.local/final-fix/original-backup.py
```

Contracts were regenerated once via `rtk proxy npm run generate:contracts`, exit0, using the existing Pydantic/OpenAPI generator; only `api.ts` and `openapi.json` changed. Its direct command output was `Generated OpenAPI and TypeScript from Pydantic (no service startup).` That direct invocation was observed in the tool output, not captured by a separate harness receipt. An initial direct Ruff pass reported five long fixture-literal lines; line splitting corrected them and final harness Ruff passed. The final documentation increment is checked by diff/consistency inspection, with no new tests.

Original backup input: `C:/p/evidra-zotero/.local/native-smoke/task12-native-exports/backup-with-pdfs.zip`,205,810 bytes, SHA-256 `a49bdb6babf47ed1f1ce8a87f884a348d34aa463a828fb47a9312306a6b22e4a`. In-memory projected CSV hashes: results `d3bfd7da86b27d2fe28fc95fe7be69b213f7cae5d48b22a8e254681de12dccfe`; studies `8de767c57bb9caa42f7993074e13412cb1f1fb0cf5b1527cc800cee7f7159a7d`. No exported fixture files or native profile were changed.

## Commits and self-review

- `55cb9d37e8b41876d59e4603e0a8e7efda8af06d` — snapshot/source-version CSV projection, existing regression extended and CSV semantics documented; focused4 cases/Ruff passed.
- `4d9d505f460f70e6009697b4fcb54bcd9c68c95e` — bind the failing-keyring test to its actual database, verify metadata persistence; focused1 case passed.
- `8c7739b39eafdffb0f6991c4a66738c06023e9ec` — bounded textual ingestion, generated contract, M1/M2, format/native-navigation documentation and covering existing/new parameterized tests.
- The commit containing this report contains only the final STATE/ACCEPTANCE_MATRIX/TEST_REPORT/report documentation increment; its identifier is reported at handoff (a commit cannot embed its own hash).

Workflow-harness Git preflights are `.local/final-fix/git-preflight-{initial,csv,keyring,text,docs}.json`; each records status, branch, root and worktree inspection. Diff checks/staging were restricted to owned files. Source completion was reported to root immediately after8c7739b, before any successor package. Root retains native helpers/receipts/ledger ownership. No branch/worktree switch, push, stash, cleanup, release or history rewrite occurred.

The controller then authorized minimal acceptance-document reconciliation using its independent package3 native summary: `.local/native-smoke/task12-native-controller-summary.json`, SHA-256 `524e1f5817d5c16d5f149ce72fc9adaf45c999b995debef1dc5043bc4e6dc88e`. It records19 observer checks and46 saved-artifact checks for four exports, explicit1/1 compound source/PDF mapping,44 imported records and successful imported original PDF evidence at page index1. Scoped matrix/Tab/MCP visual observations, normal owned host/engine cleanup and restored smooth-scroll preference are recorded in STATE and TEST_REPORT. An additional default-animation documents.open/panel-close observation remains inconclusive, and long-ID horizontal overflow remains Minor debt. ACCEPTANCE_MATRIX retains broad PARTIAL statuses and distinguishes these package3 results at08d from later final-fix source8c7739b. This documentary update neither runs native helpers nor changes the controller's receipts, model settings or acceptance gates.

Self-review traced the native kind/MIME gates through file registration, worker dispatch, immutable version publication, retrieval, full-scan coverage, UI diagnostics and text/PDF evidence separation. Reviewed generated contract changes and absent-MIME hashing; reviewed CSV grouping/source association/form lineage/import provenance and unchanged portable history. Reviewed exact test failures before interpreting GREEN, and retained original warning streams. The original task bootstrap brief/policy reads occurred before RTK policy was loaded; subsequent shell work used RTK. Later fail-closed edge cases were added to the one ingestion regression; no separate historical RED is claimed for each parameter beyond the documented initiating ingestion failure.

Remaining concerns: textual citation opening has no verified native location; some admitted MIME/EPUB features explicitly fail as documented; no model/native text-ingestion, production-scale text/EPUB benchmark or successor frozen-package validation occurred. Existing native/model/clean-Windows/installed-client/offline/full-UI acceptance gaps remain owned by the controller's acceptance record. Root's independent package3 native run is against08d, not this source wave; its receipts and any inconclusive extra visual observation must remain separate. M5 warning/capture maintenance stays deferred. No complete-v1 or unrestricted-release claim is made.

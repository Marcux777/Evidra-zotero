# Task6 implementation report

Base: `e9a6d413824a723013627516e3c9d8594d963c50`. Implementation is ongoing; this is not full M3/native acceptance.

## Backend increment

Implemented scoped exact cosine retrieval with compact little-endian float32 SQLite blocks, authorization joins before scoring/top-k, no persistent RAM vector cache, atomic active-generation swap and durable cancellable index-build progress. Defaults remain lexical40/vector40, RRF k60, at most12 document-diverse nonoverlapping chunks. The complete history is retained or context preparation fails; selected/excluded chunk counts and UTF-8-plus-framing estimate are explicit. Estimates never become monetary `InputBound` receipts.

Conversations bind snapshots. Prepared runs persist question, complete included history, server evidence excerpts/offsets/version/source, prompt/version, selected provider/configuration, effective Ollama options, context exclusions and destination. Explicit start checks scope/history/provider revisions and dispatches once; retries return the durable existing run. Events are bounded authenticated SSE cursor batches. Final promotion requires provider terminal completion/cleanup, local strict schema and literal evidence validation, current scope and conversation revision. Failed/cancelled/incomplete/fabricated output stays draft. No repair command or automatic repair. Visual preparation consumes byte-verified server preview operations and persists image provenance as proposed interpretation.

Pydantic conversation/provider native commands and generated Ajv allowlists are included for the next UI increment. No duplicate provider transport, credentials store or usage ledger was added. Existing lexical/evidence reads now honor the server-issued context capability, allowing commit-scoped consumers to use them without nesting transactions.

### Checks and actual evidence

New permanent file: `services/engine/tests/test_conversations.py`, ten focused cases. No existing tests changed or removed in this increment. Covered distinct risks: schema/evidence promotion and history persistence; fabricated ID and quotation; incomplete provider terminal; cleanup failure; cancellation; revocation during stream; unauthorized dominant vector before top-k and model/dimension mismatch; bounded context/history; late rebuild failure preserving the previous index. Providers in these checks are controlled async fixtures; source text, scope and SQLite persistence are real.

Initial RED command: `rtk proxy uv run --project services/engine --no-sync pytest services/engine/tests/test_conversations.py -q`. Actual result before implementation: `assert 404 == 201` at conversation creation; `1 failed, 1 warning in 1.21s`. The new route was absent as expected. Subsequent integration diagnostics identified schema head still5 (`sqlite3.OperationalError: no such table: vector_jobs`); schema is now6. Test additions for distinct invariants followed the consumer implementation; those additions do not claim separate preimplementation RED evidence.

GREEN command: `rtk proxy uv run --project services/engine --no-sync python .local/task06/check.py backend-increment uv run --project services/engine --no-sync pytest services/engine/tests/test_conversations.py -q`. Actual child exit0: `10 passed, 1 warning in 4.42s`. Full original captured log: `.local/task06/20260906T124525813977Z-backend-increment.log`, corresponding JSON records command/cwd/child exit. Warning is upstream Starlette's deprecated `anyio.abc.BlockingPortal` alias; not pristine output.

Changed backend mypy passed (8 files) before command-contract additions. Changed backend/test Ruff passes after formatting. Pydantic→OpenAPI→TypeScript/Ajv generation passes. Generation initially rejected OpenAPI `format=password`; it is now explicitly recognized as an annotation while the SecretWrite bounds remain validated. No live provider, vision or native Zotero check is claimed.

Commits and final UI/check evidence will be appended after each verified increment.

## Connected UI and bounded native consumer increment

Backend increment commit: `471052e`. This increment connects all six existing provider adapters through typed settings (LOCAL/API, explicit catalog/manual model, capability provenance, secret storage, global API block, consent, prices and scoped monetary caps). Conversation controls provide explicit prepare/start, complete context/history/destination preview, semantic-build progress, scoped article/excerpt actions, streaming draft, stop/resume, history and usage. Existing server-issued evidence opens through the byte-verified native Reader path. Completed claim prose uses the existing markdown-it/DOMPurify sanitizer; draft protocol text and literal quotations remain escaped text. PT-BR/en-US labels are present. No model call occurs on mount. The visual selection uses a server preview operation; unsupported vision is visibly disabled and successful interpretation remains a proposal without human approval.

Root approved the following Task6 seam: `/runs/{id}/access` derives dependencies from persisted run evidence, historical evidence and visual document versions. It returns full snapshot/grant-authorized contents for each dependent source, preserving Task4 sibling-sync semantics, plus exact document keys limiting PDF registration. Native events/read/start revalidate only those dependent sources and files, with pre/post epoch and invalidation barriers. Full startup/prepare scope validation remains. A scoped status-only cancellation bypasses the serialized content queue and emits no context/evidence/draft. Per-iframe run ownership cancels outstanding starts on view cleanup, including uncertain start responses. Historical dependencies and every prepared evidence dependency are checked again before final promotion. Vector scoring now iterates ordered SQLite membership blocks once with bounded top-k accumulation.

New test files this increment: `conversation-stream.test.ts`, `conversation-ui.test.tsx`. Changed existing tests: `engine.test.ts`, `documents-bridge.test.ts`, `test_conversations.py`. No tests removed. Reused `native-boundaries.test.ts`; prior connected-UI check also executed existing `documents-ui.test.tsx`. Stream command test initially returned `INVALID_UI_MESSAGE` before native validator integration; this was the expected observed RED. Later focused bridge regression was added after implementation and does not claim a separate preimplementation RED.

Actual Task5 registry/adapter/usage consumer coverage uses controlled HTTP via httpx MockTransport, real SQLite, and real local PDF parsing/rendering for image input. Text and image scenarios each dispatched once and recorded CONFIRMED usage with run/job/conversation identities. Image base64/hash matches the server preview and output stays a visual proposal. A versioned-price monetary cap fails `TOKEN_BOUND_REQUIRED` before any provider HTTP because the UTF-8 context estimate is not a trusted monetary bound. Initial consumer run had two fixture failures (missing PriceConfig.source and a no-evidence query rejected by preparation), retained at `.local/task06/20260906T131941587683Z-consumer-boundaries.log`; corrected focused run passed4 at `.local/task06/20260906T132026031639Z-consumer-corrections.log`.

Final changed-backend command: `rtk proxy uv run --project services/engine --no-sync python .local/task06/check.py final-backend uv run --project services/engine --no-sync pytest services/engine/tests/test_conversations.py -q` → child exit0,13 passed in6.59s. Log `.local/task06/20260906T132246352116Z-final-backend.log`. One upstream Starlette Anyio deprecation warning remains.

Native/renderer command: `rtk proxy uv run --project services/engine --no-sync python .local/task06/check.py native-stream npm test -- documents-bridge.test.ts conversation-stream.test.ts conversation-ui.test.tsx engine.test.ts native-boundaries.test.ts` → child exit0,47 passed across5 files. Log `.local/task06/20260906T132120995302Z-native-stream.log`. Its seam regression checks complete authorized sibling sync, one relevant PDF registration, no full snapshot identities enumeration, cancellation completing while the content preflight is blocked, and epoch-change rejection. These are controlled native APIs, not installed Zotero acceptance.

Strict changed-path mypy12 passed: `.local/task06/20260906T132210462064Z-final-types.log`. TypeScript noEmit passed: `.local/task06/20260906T132215669755Z-final-typescript.log`. Changed Python/test Ruff passed. Contract generation passed, and diff whitespace validation passed with ordinary repository LF/CRLF notices. Full commands/cwd/child exit codes are preserved in companion JSON receipts; no broad repeat of Task4/Task5 tests was used.

SDD state: implementation/source checks ready for root review; native Zotero/live Ollama consumer acceptance remains root-owned and not claimed here. No paid-provider/LM Studio live check, real vision model check, performance benchmark or 1,000-article/50,000-chunk acceptance is claimed. Automatic repair remains zero. No unrelated artifacts, native applications, runner processes or personal-library state were modified by this worker. Root will bind the final source commit and unique XPI/engine hashes in the native acceptance receipt.

Plugin build passed with12 resources/237753 bytes; original log `.local/task06/20260906T132505843669Z-plugin-build.log`. Unique retained XPI: `.local/task06/artifacts/20260906T132507234960Z-task6-final/evidra-0.1.0.xpi`, SHA256 `0ca92645d6495ffce2355c8c828e1a190af2a98e3d5fc4d3487bfe9a5ecb369f`. Adjacent `build-receipt.json` records exact esbuild inputs/outputs and SHA256 `d62fd6f52bbd8bdc083d6566b35371de28b92ae2e7ec0c1c676eed51f2ad6ffe`. The previous shared output was copied to the unique `20260906T132505277778Z-prebuild` directory before rebuilding; it was not discarded. No engine freezing or native restart was performed by this worker.

## Review fix round1 (I1–I3, M1)

Reviewed source was `9d3a98990c6a447cdccd57a38e25afc7200c5fa8`; independent findings remain verbatim in `task-6-review.md`, SHA256 `a37aa9598bdd26381a734d030a20f352627ce4f9676817fd55e612807777a5a6`. Before updating this report, byte-identical copies of the report, review, original manifest and all original final artifacts/receipts were retained under `.local/task06/fix1-originals/` with hash-verified `retention-receipt.json`. Original unique artifacts and original manifest remain unchanged.

I1: complete historical answers now carry forward their excerpt/metadata classifications, and historical citations independently require both categories. History does not replay earlier image bytes, so old image classification alone is not carried forward. This closes a visual continuation with no current lexical hits sending historical quotations under image/history-only consent. The controlled regression executes a real sourced local answer, creates its real PDF preview, selects an API profile and grants history/images/metadata while withholding excerpt consent. Before the fix it dispatched the historical quotation to MockTransport; after the fix Start returns403 `CONSENT_REQUIRED`, the run stays PREPARED and zero cloud HTTP requests occur.

I2/I3: the Conversation component tracks only run IDs it submitted through Start, before awaiting the result. Unmount cancels those owned IDs, including pending/uncertain Starts, while retaining native iframe cleanup as the final guard. Reopening an unrelated RUNNING run does not acquire ownership. Stop has its own duplicate guard/state and can submit status-only cancellation while Start holds the ordinary action lock. Stop/unmount invalidate the pending Start response, which cannot populate a final answer or restart polling afterward. Cleanup cancellation failures retain native diagnostics and are also reported to the renderer console. M1 uses existing chat state labels for history/header and document-operation labels for vector indexing; it never labels indexing as a validated answer. M2 warning debt is unchanged.

Only four executable/test files changed: `services/engine/src/evidra/conversations/service.py`, `services/engine/tests/test_conversations.py`, `apps/zotero/src/ui/Conversation.tsx`, `apps/zotero/tests/conversation-ui.test.tsx`. No new permanent test files or dependency/hook changes. Existing tests were extended at the uncovered consent and component-lifecycle boundaries. Original RED: `.local/task06/20260906T133949484797Z-fix1-red-consent.log` records the actual unauthorized controlled dispatch. `.local/task06/20260906T133953408735Z-fix1-red-lifecycle.log` records the three missing-unmount-cancel failures; that initial Stop lookup used the wrong English label. Corrected Stop RED `.local/task06/20260906T134017072553Z-fix1-red-stop.log` fails `expected true to be false` on the disabled button. The local capture helper had already saved its original log/child exit receipt when printing Vitest Unicode to cp1252 failed; its stdout was then set to UTF-8. No child test result was discarded or converted to success.

The first post-fix consent test expected asynchronous200 failure, but existing Start authorization correctly refused synchronously with403; this diagnostic remains at `.local/task06/20260906T134131349008Z-fix1-green-consent.log` (child exit1, despite the provisional label). Corrected covering check `pytest services/engine/tests/test_conversations.py -q -k historical_consent` passes1,13 deselected at `.local/task06/20260906T134154633860Z-fix1-consent-preflight.log`. `npm test -- conversation-ui.test.tsx` passes5 at `.local/task06/20260906T134134785856Z-fix1-green-lifecycle.log`; the subsequently added ownership-only case passes1,5 skipped with `-t unmount-observer` at `.local/task06/20260906T134307802630Z-fix1-observer-ownership.log`. These checks prove Stop during pending Start; unmount during running/pending/uncertain Start; no late promotion; and no cancellation of a merely observed run.

Strict mypy for the changed service passes1 source file (`.local/task06/20260906T134310915036Z-fix1-mypy.log`); TypeScript noEmit passes (`.local/task06/20260906T134312064863Z-fix1-typescript.log`); changed Python/test Ruff and diff inspection pass. Prior13/47 checks were not rerun for ceremony. All full command/cwd/exit receipts remain adjacent to their original logs. Final fix source commit and unique build artifact hashes are recorded in `.local/task06/task-6-fix1-evidence-manifest.json`; the new output is built into a fresh directory without overwriting prior artifacts. Source fixes await root re-review; installed Zotero, engine freeze and live model acceptance remain root-owned and unexecuted here.

## Native fix round2

Base: `d539dd11675789d3359af0d9d3b0280d5fee4148`. Root's original native failure remains recorded separately in `.local/native-smoke/task6-native-initial-failures-summary.json`; this worker did not alter that directory or execute native/model operations. Before appending this section,21 prior report/review/manifest/build/receipt files were copied and hash-verified under `.local/task06/fix2-originals/retention-receipt.json`. Prior source reports, unique XPIs, manifests and failure evidence remain available.

The provider's pure schema planner now runs during context preparation. The record persists the effective system, complete unchanged output schema and selected schema mode. The estimator counts the effective system once, plus the separate native-format schema when applicable, complete history, prompt, framing and image allowance. Dispatch checks the prepared mode against the current adapter/schema capabilities and uses the stored effective system unchanged; local schema instructions cannot be appended a second time. PT-BR/en-US show a concise response-format mode and inspectable schema alongside exact system/history/prompt. The original estimate already included the970-byte schema: only the40-byte instruction prefix was omitted, although the entire1,010-byte addition was hidden from the preview. This fix does not attribute the native generation failure to that40-byte difference or claim a measured tokenizer count.

Only Ollama adds `minLength`, `maxLength`, `minItems` and `maxItems` to its conservative native subset; all original constraints and mandatory local validation remain. Root verified the pinned primary chain: [Ollama0.33.3 schema forwarding](https://raw.githubusercontent.com/ollama/ollama/v0.33.3/llm/llama_server.go), [pinned llama.cpp revision b10760](https://raw.githubusercontent.com/ollama/ollama/v0.33.3/LLAMA_CPP_VERSION), and [converter bounds at lines978–997](https://raw.githubusercontent.com/ggml-org/llama.cpp/b10760/common/json-schema-to-grammar.cpp). [Official structured-output documentation](https://docs.ollama.com/capabilities/structured-outputs) describes the full-schema format field. Other adapters retain their earlier subset; unsupported constraints still select complete schema instructions and local validation.

Ollama now records terminal state and validated terminal usage before rejecting a non-stop outcome. The common generator emits confirmed terminal usage, then raises `GENERATION_INCOMPLETE` before JSON validation/final promotion. Valid available counts therefore reach the ledger even when generation or response cleanup fails; missing counts remain null with `BILLING_UNKNOWN`. A dedicated exception retains only an ASCII protocol identifier of1–64 characters (`unrecognized` for other values). Registry cause diagnostics and the ordinary conversation failure record retain this sanitized reason; no raw frames or provider reasoning are persisted by diagnostics. Existing cleanup semantics remain, including cleanup failure taking precedence while the registry logs the original termination cause. No output/context/model/seed/precision setting, retry or repair policy changed.

Historical records without schema-plan fields deserialize those fields as null, and retain their old system/estimate/error/state without reconstruction or migration. Completed/failed records stay completed/failed. An old PREPARED record returns409 `SCHEMA_PLAN_MISSING` on Start, without a provider request or durable mutation; the UI disables sending and asks for a new preparation. This is an explicit missing historical record, not an inferred native/local mode. Existing conversation regression verifies the old prepared payload remains byte-content equivalent after refusal.

Changed existing tests only. Initial plan RED: `.local/task06/20260906T140642651098Z-fix2-red-plan.log` (missing schema_mode); terminal RED: `.local/task06/20260906T140743525295Z-fix2-red-terminal.log` (missing termination reason); mode UI RED: `.local/task06/20260906T140820438375Z-fix2-red-mode-ui.log` (native mode absent). Focused green checks: actual registry/native and local-validation plan equality, conservative estimate equality, actual visual bytes, monetary preflight, historical consent and length with/without usage plus context limits passed8 (`20260906T141049738381Z-fix2-consumer-context.log`); six adapter subset/local-validation cases passed6 (`20260906T141050406234Z-fix2-adapter-subsets.log`); schema-visible UI passed1 (`20260906T141050262487Z-fix2-mode-ui.log`). The subsequently added historical refusal assertion passed its one existing scenario (`20260906T141234842930Z-fix2-historical-preservation.log`). Two registry length/cleanup cases passed2 (`20260906T141235018802Z-fix2-terminal-cleanup.log`), proving confirmed8/4 usage, response closure, no final result and retained reason even when cleanup fails. Source mypy passed8 files (`20260906T141050946088Z-fix2-mypy.log`), TypeScript noEmit passed (`20260906T141050654881Z-fix2-typescript.log`), generated contracts and changed-file Ruff passed. Full logs and adjacent command/cwd/exit receipts are under `.local/task06/`. One previously recorded upstream Starlette/Anyio deprecation warning persists; broader suites were not rerun. Exact commit, unique XPI, source input hashes and build receipts are bound in `.local/task06/task-6-fix2-evidence-manifest.json` after the verified source commit. Root review and native/live acceptance remain pending.

# Acceptance matrix

Statuses require executed evidence. PARTIAL does not pass the complete criterion. Tasks 1–11 have scoped source approval. Task 12 adds bounded cache eviction, UI diagnostics/layout fixes, distribution scripts and a declared CPU benchmark. Final package/native review remains in progress. The three original Task 9 model runs each failed GENERATION_INCOMPLETE at unchanged 4096-token caps and accepted no artifact; those failures are preserved. All 36 rows below were reconciled with the available source, fixture and native receipts. A source implementation or fixture success is never substituted for a missing native/clean-machine result.

Task 11 native receipt: `.local/native-smoke/task11-native-controller-summary.json`, SHA-256 `862d9b746eac7a97988e5af00ddab9a74f6d2bc5fbccc173173e44bbdf0a287c`. It records nine real saved artifacts and 43 backup/bibliography checks, plus two actual defects: CSV mislabeled a retained approved cell after rejecting a competitor, and backup inspection rejected that valid history. Commits `b40c224` and `af69b4a` fix those source defects with a focused regression; the original artifacts remain unchanged.

The subsequent package-3 native receipt is `.local/native-smoke/task12-native-controller-summary.json`, SHA-256 `524e1f5817d5c16d5f149ce72fc9adaf45c999b995debef1dc5043bc4e6dc88e`:19 observer checks and46 saved-artifact checks, four exports, explicit1/1 compound source/PDF mapping,44 imported records, and original imported PDF evidence at page index1. Native matrix labels, pagination, scoped Tab focus and MCP provenance/status labels were observed. A separate default-animation opening/panel-close observation was inconclusive; horizontal overflow for long imported IDs was observed there and is fixed/verified in package4 below. This receipt binds package3 at `08d3cf6`, not final-fix source `8c7739b`. Later text-ingestion and snapshot-local CSV source checks are documented in [TEST_REPORT.md](TEST_REPORT.md); no native result is inferred for them. Broad row statuses remain partial where their full criterion is unverified.

Resumed criterion audit (2026-09-07): **26 VERIFIED_WITH_FIXTURES, three bounded VERIFIED_LIVE, seven PARTIAL**. SPEC section 23 permits real SQLite/property/protocol fixtures for the corresponding invariants; it does not require every clause to repeat as a native scenario. The independent audit is `.superpowers/sdd/IMPLEMENTATION_PLAN/resumed-acceptance-audit.md` (SHA-256 `a9cef3e7947e7eea012d74fef973de169c658bc307ab85b35a30de79ba66e10f`); the controller separately closed its narrow A02/A10 fixture gaps with `.local/task12/resumed-selection-receipt.json` and `.local/task12/resumed-generator-switch-receipt.json`. Each row retains its native limitations. A26 does not require paid live calls, and A27 does not require installed desktop clients. The explicitly required A25 network isolation and A33 clean Windows checks remain partial, as does the known A23 native cancellation failure. Package4 closes the A01 lifecycle and A12 original-document gaps with the bounded native receipts below. No failed model run becomes successful through this classification, and v1 remains incomplete.

| ID | Requirement | Files | Check / receipt | Result |
|---|---|---|---|---|
| A01 | XPI carrega no Zotero-alvo, exibe telas/menus e é removido sem listeners/processos órfãos. | `apps/zotero/src/bootstrap`, bridge, UI, XPI | Reviewedb132506 passed native admission/menu/opaque UI/consent/notebooks, restart and two-window synchronization. Package4 at c2ae541 adds composed actual disable/reenable/uninstall: registered source notifier/menu/section and owned content removed, exact engine/child absent, saved notebooks reopened after consent. Final active-source uninstall passed15 checks and normal host exit. resumed-native-lifecycle-controller-summary.json preserves the failed intermediate uninstall assertion and accepts only the independently proven scopes. | VERIFIED_LIVE (bounded) |
| A02 | Selecionar múltiplas coleções usa APIs plurais e não confunde cabeçalhos da lista com itens. | `sources/resolver.ts`, Task3 source bridge | Actual plural collection/saved-search union passed in task3-native-collections-corrected-receipt.json. The focused resumed-selection-receipt.json executes the production selector with controlled plural APIs: grouped-header item-reference getter is never read, collection rows never become whole-library selections, and only an actual library row adds library7. The grouped-header result is a contract fixture, not a native screenshot. | VERIFIED_WITH_FIXTURES |
| A03 | Item em coleções sobrepostas entra uma vez; itens homônimos de bibliotecas diferentes não se fundem. | Task3 compound identities and snapshot membership | Actual overlapping collections include Shared exactly once; SQLite/property compound-identity cases pass. Native homonyms across group libraries remain unverified. | VERIFIED_WITH_FIXTURES |
| A04 | Subcoleções/filtros/buscas salvas/exclusões produzem exatamente o snapshot esperado. | Task3 selection/staging/snapshots | Exact fixtures plus actual collection/search union, descendant opt-in, inclusive year, AND tags and exclusion yield 3/4/5/2/1/0 expected preview members and a persisted empty snapshot. task3-native-collections-corrected-receipt.json; remaining native attachment-specific permutations are unverified. | VERIFIED_WITH_FIXTURES |
| A05 | Fonte muito relevante fora do escopo nunca chega ao prompt, às evidências, exports ou MCP. | Central scope, retrieval, export and MCP | SQLite/FTS/evidence, scoped export/backup and MCP fixtures enforce current authorized sources. Native conversation/MCP returned selected Cedar evidence and excluded Willow. Task11 native JSON/Markdown/backups preserve declared scope; Task12 package3 passes the corrected bounded export/import flow. The evidence composes the exercised boundaries; a single adversarial native cross-flow trace is unrun. | VERIFIED_WITH_FIXTURES |
| A06 | Restrição ocorre antes do top-k: itens globais fora do escopo não escondem resultados válidos internos. | Scoped lexical and semantic retrieval | Existing real SQLite pre-top-k fixtures retain authorized hits over globally higher-ranked foreign evidence. Task12 CPU benchmark uses the actual scoped lexical/vector implementations at 1,000 sources/50,000 chunks; scope is resolved before ranking. See .local/task12/benchmark-3/report.json. | VERIFIED_WITH_FIXTURES |
| A07 | ID adivinhado de evidência/snapshot de outro caderno recebe recusa. | Shared scope, evidence and MCP gateway | Real SQLite/HTTP foreign snapshot/source/evidence/principal refusals pass. The official SDK against the actual native frozen MCP package also rejected invented evidence IDs and foreign notebook arguments. The scoped refusal invariant is verified at the recorded HTTP/SQLite and native SDK boundaries. | VERIFIED_WITH_FIXTURES |
| A08 | Remoção de fonte durante geração impede commit no novo escopo; cache não ressuscita o conteúdo. | Shared scope, ingestion, model runs and current-content cache checks | Task 4 cache/registration and Tasks 6/8/9 guarded-run fixtures block stale/revoked content commits. Native material changes invalidate evidence. Native in-flight generation revocation remains unverified. | VERIFIED_WITH_FIXTURES |
| A09 | Revogação de biblioteca e fechamento do bridge bloqueiam novas leituras MCP. | Heartbeat, native source invalidation and MCP connection guard | Task 10 SDK/HTTP/SQLite fixtures deny expired/revoked connections, changed source access and heartbeat age31s. Native explicit UI connection revocation denied both already-connected SDK clients immediately. Actual Zotero closure/library notification to an external client remains unverified. | VERIFIED_WITH_FIXTURES |
| A10 | Fonte/metadado alterado fica stale; trocar modelo gerador não exige reindexar. | Immutable source/document versions and separate retrieval/model identity | Native metadata/file mutation and reindexing checks retain immutable versions and reject stale evidence. resumed-generator-switch-receipt.json changes the real provider profile from fixture to fixture-generator-two, prepares a new conversation with identical original evidence, and verifies all document_versions/pages/chunks/FTS rows unchanged. Zero provider calls; no inference success is inferred. | VERIFIED_WITH_FIXTURES |
| A11 | Parsing registra todas as páginas ou falhas explícitas; PDF sem texto não recebe falsa análise integral. | Task4 PDFium supervisor/worker/immutable pages | Real PDFium/SQLite checks cover all-page accounting, limits, blank/image-only/corrupt/unmapped fixtures and explicit coverage. Native runs account for2/2and1/1pages with exact1054/1551bytes. task-4-report.md and native core/page receipts. | VERIFIED_WITH_FIXTURES |
| A12 | Citação abre anexo e página corretos, incluindo rotação/rótulos; ausência de bbox usa fallback rotulado. | Task4 evidence/DocumentBridge/Reader | Native PDF page2/label7/rotation90/crop and24 temporary rectangles passed; page-only retains excerpt without rectangles, and equal-length cached-version refusal passed. Package3 imports.open verified original PDF/hash at page index1. Package4 closes F2: actual original TXT/XML/HTML/EPUB opening, exact XML/source text, HTML tables/figures, citation-selected EPUB resource2 with navigation and passive pixels, stale-file refusal clearing both panes. resumed-original-native-controller-summary.json binds57 scoped checks and native captures. Publisher CSS layout is explicitly omitted; the earlier extra default-animation observation remains inconclusive. | VERIFIED_LIVE (bounded) |
| A13 | Modelo que inventa evidência ou excerto recebe falha de validação, não selo de aprovação. | Conversation, extraction and research validators | Source/fixture checks reject invented evidence, invalid anchors and unsupported promotion. Actual Task 6 malformed output remained failed; Task 8 incomplete generations produced no trusted results. See task-6/task-8/task-9 reports. The invalid-output invariant is verified; successful native research generation remains absent. | VERIFIED_WITH_FIXTURES |
| A14 | Busca lexical funciona sem internet, embeddings ou LLM. | SQLite FTS and document UI | Local real-process/SQLite checks and actual Zotero search work without configured generation or embedding models. External-network-blocked native execution remains the explicit separate A25 gate. | VERIFIED_WITH_FIXTURES |
| A15 | Busca semântica rejeita dimensões/modelos incompatíveis; seu filtro de escopo passa nos mesmos testes. | Scoped semantic index and retrieval | Existing Task6 tests reject changed model/digest/dimension and limit membership before top-k. Task12 measures real CPU vector retrieval over 50,000 seeded synthetic 384-dimensional float32 vectors; no learned embedding/semantic quality claim. | VERIFIED_WITH_FIXTURES |
| A16 | Job sistemático enumera todos os estudos e discrimina leitura integral/parcial e arquivos ausentes. | Extraction job preparation, coverage and scheduler | Task 8 source/fixture checks enumerate studies and coverage; native preparation passed 22 observations. Actual model extraction failed with GENERATION_INCOMPLETE and no trusted results. Native successful extraction remains pending. | VERIFIED_WITH_FIXTURES |
| A17 | Informação ausente produz null/estado correto, não zero, suposição ou resultado inventado. | Typed matrix values and research validation | Task 7 real SQLite/component checks and native manual matrix preserve explicit null/state values through restart. Task 9 native preparation preserves missing abstract uncertainty. Model extraction acceptance remains pending. | VERIFIED_WITH_FIXTURES |
| A18 | Célula aprovada não é sobrescrita por reextração, race de job ou proposta MCP. | Matrix decisions, extraction, export and MCP proposals | Decision/revision/lineage fixtures preserve approved values under competing proposals. Native reviewed matrix survived competing rejection and restart. Task12 package3 CSV/backup/import preserves approved87.5 distinctly from rejected88. Later snapshot-local CSV fixtures preserve the selected snapshot/source version; their native run remains unverified. Full extraction-race native acceptance remains partial. | VERIFIED_WITH_FIXTURES |
| A19 | Resultados múltiplos preservam dataset, unidade, baseline e condição. | Typed contextual extraction results and matrix UI | Task 7 SQLite and native manual matrix checks preserve multiple result contexts, units and accepted values. Actual model extraction acceptance remains failed/pending. | VERIFIED_WITH_FIXTURES |
| A20 | Triagem mantém parecer IA separado de decisão humana e critério versionado. | Versioned protocols and separate human/model screening | Task 9 research fixtures cover model/human separation. Native preparation retains two protocols and local reviewer conflict/correction. The actual screening call failed GENERATION_INCOMPLETE at 4096 output tokens, with no artifact or human-decision promotion; task9-native-screening-m6-controller-summary.json preserves usage, exact preview and normal shutdown. Successful native AI screening remains unverified. | VERIFIED_WITH_FIXTURES |
| A21 | Auditoria distingue âncora válida de sustentação proposta; referência citada indiretamente não vira artigo lido. | Audit validators and versioned human corrections | Task 9 fixtures cover invented anchors, indirect/unavailable references, support states and corrected references. Native audit started the preserved exact claim/original scoped evidence once; GENERATION_INCOMPLETE at 4096 output tokens left no artifact. task9-native-audit-m6-controller-summary.json preserves the failure and shutdown. Successful native audit output remains unverified. | VERIFIED_WITH_FIXTURES |
| A22 | Interrupção/reinício retoma unidades confirmadas sem duplicar extrações ou notas. | Job/research checkpoints and note outbox | Task 9 real child-process interruption fixtures distinguish SENT from validated checkpoints; outbox fixtures cover reconciliation. Native restart preserved all three exact prepared previews before their one attempted call each; all failed generation and produced no accepted checkpoint/artifact. Successful research generation, approved note publication/reconciliation and their restart remain unverified. | VERIFIED_WITH_FIXTURES |
| A23 | Cancelamento impede novas chamadas e mostra custo incerto quando a chamada já foi enviada. | Provider accounting and job/research cancellation | Native Task 6 conversation cancellation passed with unknown usage. Native Task 8 job cancellation failed with REVISION_CONFLICT and two admitted calls; reviewed source fix 810a564 is installed through Task 9 but not yet native revalidated. | PARTIAL |
| A24 | Limite/erro de provider não ativa API paga, outro provedor ou outra credencial. | Provider registry admission and explicit profiles | Task 5 protocol/accounting fixtures and later run fixtures retain provider identity and failures without substitution. Three native research calls failed at their preserved output caps with one call each, no retry/substitution and no accepted artifact. Paid-provider live smoke remains unrun. | VERIFIED_WITH_FIXTURES |
| A25 | LOCAL rejeita endpoints/modelos explicitamente remotos e funciona em smoke com rede externa bloqueada. | LOCAL endpoint/model policy and Ollama adapter | LOCAL rejection fixtures pass; real scoped native conversation used the authorized loopback Ollama models. External-network-blocked execution remains unverified. | PARTIAL |
| A26 | Cada adaptador tem teste de protocolo correto, streaming, schema inválido, quota, timeout e cancelamento. | Six provider protocol adapters | Task 5 reports retain controlled protocol/stream/schema/error/timeout/cancellation coverage. Ollama generation/embedding had synthetic live smoke; other provider live smoke remains unverified. | VERIFIED_WITH_FIXTURES |
| A27 | MCP só serve o caderno autorizado; sessão read-only não cria propostas; nenhuma ferramenta aplica notas. | Eight-tool MCP gateway and scoped external proposals | Task 10 final scoped review passed at 5939063. Actual native Zotero plus the official SDK against the frozen stdio server passed scoped reads, read-only proposal refusal, pending external-note provenance, an eight-tool map without note application, and same-client denial after UI revocation. task10-native-controller-summary.json binds 21 native and 18 SDK checks. Package3 visually distinguishes revoked/expired connections and an unreviewed external note with a declared-unverified model. Installed Codex/Claude/Gemini clients remain unverified. | VERIFIED_LIVE (bounded) |
| A28 | HTML/PDF com prompt injection não expande fontes, executa scripts, lê arquivos nem aciona escrita. | Typed bridge, opaque renderer, parser, model validators and external proposals | Native opaque sandbox and scoped parser checks pass. Package4 hostile original HTML/EPUB checks retain passive structure/figures with no additional foreign attachment lookup or external request, no source scripts/URLs and explicit omissions. The controller summary preserves the failed bundled Reader policy canary. Research fixtures reject invented anchors; MCP accepts literal untrusted text without granting write authority. Complete adversarial model/export cross-flow acceptance remains pending. | PARTIAL |
| A29 | Host/Origin indevido, token ausente e symlink/path forjado são recusados. | Runtime/handshake and Task4 registry | Auth/ACL/startup and actual registered-file sharing/junction/path/byte substitution refusals pass. File-symlink setup remains SKIPPED after WinError1314; no privileges changed. | PARTIAL |
| A30 | Segredos não aparecem em logs, backup, argumentos de processo ou configurações exportadas. | Private runtime/provider/MCP credentials and sanitized exports | Runtime/provider/MCP fixtures keep credentials out of argv, logs and portable models. Task11 actual backup/bibliography checks exclude runtime secrets and original attachment paths. Final packaged-process smoke2 checks session secrets absent from both process argv and captured engine logs. | VERIFIED_WITH_FIXTURES |
| A31 | CSV neutraliza fórmulas textuais; backup malicioso não escreve fora do destino. | exports/csv.py, exports/backup.py; test_exports.py | Real SQLite/HTTP export tests exercise formula-neutralized text, typed numbers, path traversal, linked entries, size/ratio limits, checksums and strict portable schemas. Actual native bibliography projection removed excluded items/children before each translator. Package3 corrected CSV/backup artifacts passed independent inspection; hostile-backup safeguards retain fixture verification. | VERIFIED_WITH_FIXTURES |
| A32 | Backup/restauração preserva decisões e versões; não presume que IDs de outro perfil são válidos. | Versioned PortableNotebook and explicit import mappings | Existing multi-generation real SQLite roundtrips preserve immutable history without adding local grants or write authority. Task11 INVALID_BACKUP remains a recorded original failure. Package3 passed explicit1/1 compound source/PDF mapping, import of44 records, retained approved/rejected history and original PDF evidence opening. Broader native cross-profile permutations remain unverified; separate-profile SQLite mapping fixtures cover the identity invariant. | VERIFIED_WITH_FIXTURES |
| A33 | Engine empacotado roda em Windows limpo sem Python/Node, com protocolo compatível. | Preliminary Windows PyInstaller engines through M6 | Reviewed Windows onedir builds through M6 ran actual Zotero parsing/search/conversation/matrix/research preparation and official MCP SDK lifecycle. A prior standalone smoke used child PATH restricted to System32. Task12 final package at .local/task12/package-2 passes16 frozen-process smoke checks with child PATH restricted to System32, two normal heartbeat-expiry exits, PDFium and durable matrix state. A clean Windows host remains unverified. | PARTIAL |
| A34 | UI distingue fonte indisponível, falha, resultado parcial, rascunho e dado aprovado; teclado funciona. | React locale diagnostics, matrix virtualization and native UI | Task12 checks locale parity/UTF-8, named controls/landmarks, focus styles and50,000-row keyboard virtualization. Package3 observed readable labels/pagination, distinct approved/rejected values and MCP provenance/status. Package4 verifies imported-ID wrapping with clientWidth equal to scrollWidth for viewport/history/row/detail and visible native captures. Native original dialogs preserve Tab containment, Escape closure/focus return and causal stale-file diagnostics. Complete keyboard/accessibility remains unverified. | PARTIAL |
| A35 | Performance real medida com corpus e hardware declarados, sem números inventados. | scripts/benchmark.py; actual CPU SQLite/NumPy retrieval | benchmark-3/report.json: seed7, 1,000 synthetic sources, 50,000 short structured pages/chunks, 6,567,500 original text bytes; 30 measured queries after3warmups. Combined warmed p95 1.265s, indexing8.543s, PeakWorkingSet127,111,168B, SQLite320,512,000B, restart/revalidation1.085s. Hardware and all raw samples are recorded. PDF parsing measured separately; model timings and UI300ms goal are unmeasured. | VERIFIED_WITH_FIXTURES |
| A36 | Um fluxo real completo gera uma matriz, abre uma evidência e exporta resultado a partir de Zotero. | Native scoped answer, reviewed matrix, original evidence and exports | Actual Zotero produced a cited scoped answer, saved a manually reviewed matrix, opened original PDF evidence and saved JSON/Markdown/two CSV/backups/bibliography files. Package3 passed the corrected bounded export/map/import/original-evidence flow. Native systematic model extraction remains failed; no complete model-to-matrix acceptance is claimed. | PARTIAL |

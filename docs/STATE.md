# Evidra implementation state

Updated: 2026-09-06. Owner: current Codex task in C:/p/evidra-zotero. Branch: codex/implement-evidra.

The full v1 implementation is in progress, not complete. Tasks1–5 have passed their scoped source reviews. M2 now has executed native evidence for PDF parsing/search, exact and page-only Reader navigation, local preview, annotation text, material-change invalidation and changed-file/cached-Reader refusal. Tasks6–12 and final release acceptance remain outstanding. The supplied SPEC.md is preserved unchanged and IMPLEMENTATION_PLAN.md decomposes M0–M7 into twelve tasks.

## Environment and authorizations

- User authorized implementation, local dependency installation, and updating the existing Zotero application; closed Zotero before installer execution.
- Zotero updated 9.0.6 -> 10.0.1 with official digitally signed installer; silent install returned 0; executable, registry and the personal helper's start/status verified 10.0.1 and local API/Connector HTTP 200. No personal-library content was selected or changed by the agent. The application owns its normal version upgrade.
- Installer SHA-256: `4b0f508ef28bff9a7b6cc2299eb18f2431d966d91a462ce29e5bc609d5e07036`; source https://download.zotero.org/client/release/10.0.1/Zotero-10.0.1_x64_setup.exe. Authenticode status Valid, Corporation for Digital Scholarship. Local download receipt: `.local/installers/download-receipt.json`.
- Node 22.15.0, npm 10.9.2, uv 0.11.28, actual Python 3.12.11 at C:/Users/marcu/AppData/Roaming/uv/python/cpython-3.12.11-windows-x86_64-none/python.exe. The `py -3.12` launcher failed because the registered StabilityMatrix path was stale; no interpreter substitution or new Python install was used.
- `npm ci --no-audit --no-fund` installed 126 packages; `uv sync --project services/engine --frozen --python <verified-python> --no-python-downloads` installed 63 packages. npm warned of transitive deprecated whatwg-encoding in test-only jsdom.
- Initial npm resolution failed ERESOLVE because openapi-typescript requires TypeScript ^5.x. Pinning verified TypeScript 5.9.3 resolved it; no force/legacy-peer override. Original npm diagnostic logs remain in the user's npm cache with timestamp 2026-09-05T20_32_57_449Z.
- winget lookup failed to update its source / find the initial queried package. Update used the independently verified official Zotero download, not winget. PowerShell utility/security modules were absent from autoload search; explicitly importing the installed Security module enabled signature validation. Hashing used Python hashlib.

## Current files and verification

Task 1 commit `2759afbae9638e742bf96aa2174e26b2e3efbca8` adds the authenticated loopback engine, heartbeat, private one-use Windows handshake, versioned SQLite storage and persistent/idempotent notebooks. Nine permanent test functions collect 21 cases; all passed. Ruff and strict mypy passed on 16 source files. The upstream Starlette/AnyIO deprecation warning remains visible and documented.

Review fix `a47c06e3870b8e0715dfff3a68b69fd449c36681` corrects slow-body heartbeat expiry and adds sanitized causal CLI diagnostics. The final covering run passed 18 cases (seven unrelated cases not repeated); changed-file Ruff/mypy passed. There are now 25 cases in ten functions. Scoped independent re-review found both findings addressed and no new Critical/Important breakage; see the appended fix report for exact RED/GREEN evidence.

Two real hidden engine processes exercised restart persistence, authentication, heartbeat expiration and cleanup against synthetic data. Both exited 0, consumed their handshakes, removed their receipts and closed their sockets. A built Python wheel contains the migration. This does not verify the eventual Windows executable distribution or native Zotero interface. See TEST_REPORT.md and `.superpowers/sdd/IMPLEMENTATION_PLAN/task-1-report.md` for exact logs and hashes.

Task 2's native source implementation is committed at `cae92f6ba3b982e1a685fa9bda45a537fba39a5f` with fixes at `4240646b318ad2b2a4306f85c4f00b6bbb549b35`. Independent scoped re-review confirmed fingerprint-bound executable consent, refreshed notebook lists across existing panels, and retained sanitized causal diagnostics; no new Critical/Important breakage. Final covering typecheck, 15 Vitest cases in four files, XPI build and controlled adapter-to-frozen-engine smoke passed. Actual native Zotero acceptance remains NOT_VERIFIED. The reviewed XPI is 88,142 bytes, SHA256 `f71bb79ffb2e510845c9799171d3b19f92cde448ada8e15c10c3021f5be61166`.

The first authorized native attempt FAILED before bootstrap because that XPI omitted mandatory update metadata. The corrected source at `f76e8a480cf4fe94892bc2d147fdbadf1d53f27e` passed scoped review; its 88,202-byte XPI has SHA256 `f83d58b47dcd0c1a79cc290b7b4ab6e67c0932f7ff46a7b672e8d43fd986d53f`. Actual Zotero10.0.1 then accepted the addon and its approved HTTPS empty update feed. The initial native menu timeout was a test-helper lookup error: Zotero creates the item on popupshowing and identifies it through data-l10n-id.

After correcting only the helper, the real menu opened a native dialog and loaded React in an iframe with sandbox=allow-scripts. The UI then failed with `Permission denied to access property "postMessage"`; no native notebook creation or engine-start flow passed. A separate native experiment identified an invalid multi-component PathUtils.join call during package verification. Task2 round3 fixes are committed at `a06637f0e3189dd47c4830ec9b0b91cefc4ae0b4`: 17 covering tests, typecheck and XPI build passed; independent scoped review confirmed F6/F7 addressed with no new breakage. Its actual native run still failed with React loading indefinitely. An unchanged-artifact native observer proved that the host emits document load only in capture phase on the iframe, so the original noncapture/iframe-target listener could not initialize the channel. Round4 correction652c3da passed focused checks and independent review; Task3 work has resumed with all27 partial paths preserved. Exact receipts and causes are in TEST_REPORT.md.

Root installed verified Ajv 8.20.0 and Pillow 12.3.0 as required project dependencies under the existing authorization; see DEPENDENCIES.md. A preliminary M0 PyInstaller onedir passed actual authenticated notebook/expiry/cleanup checks with child PATH restricted to System32. This advances A33 only partially: the final package, a clean Windows host and native Zotero remain pending. The initial manifest-space rejection and test-harness environment-key error were corrected with their original causes retained in `.local/native-smoke/`; neither failed attempt is counted as success.

Official target-source inspection found that unknown custom tab types can break Zotero session restore after plugin removal. ADR 0002 selects the spec's allowed native in-window panel plus supported reader section. No Zotero session methods are patched. Provider protocol research is recorded in PROVIDER_PROTOCOL_REFERENCE.md.

The user subsequently authorized installing a local model service for validation. Portable Ollama 0.33.3 and the public qwen3:4b / qwen3-embedding:0.6b models are installed under `.local/ollama`. The owned loopback server passed a synthetic live JSON-streaming/embedding smoke, both models fully on the RTX 5060 Ti GPU. This verifies the external local test environment; Evidra adapters and semantic retrieval are still later work. Exact models, digests, settings and receipts: LOCAL_MODEL_TEST_ENVIRONMENT.md. No paid API, personal-library content or model weights entered the release payload.

## Next step

Current boundary at12:25Z: Task5 source through `12b6aa71c2c8313c22ad5d38dbf6e12cdef98993` passed scoped review after correcting I1–I4 and N1. The final increment blocks cancellation during HTTP cleanup from delivering a final answer while retaining confirmed usage. The preceding134-case covering run and this increment's focused RED/GREEN plus static checks are recorded in TEST_REPORT.md; no unchanged suite was repeated. A live synthetic run on the exact source archive returned the expected JSON and finite normalized3x1024 embeddings, with both models on GPU. Receipt SHA256 `0cda3122c32a6295a6f3626acde810c0d4ce0733a3c8fa470e190408f64446ff`. Task6 now consumes these services for semantic retrieval, conversations, provider settings and native streaming. LM Studio/paid calls, actual Windows keyring, offline network isolation and native conversation acceptance remain unverified.

Historical descriptions below retain their original milestone boundaries; they are not the current status.

Current boundary at07:01Z: Task4 source revision0c1b8cfb4c9cde1ae5fc3bdc511b89ba7a413ac4 passed its third scoped re-review. The reviewed132223-byte XPI has SHA2565b63b6dd061a8f03d89e9b315242b5ed49a5d01383dddaa5f03a348cf3afdabf. Three completed native runs contain23/20/20checkpoints; all three scopes restored the temporary animation preference and closed their exact hosts/engines normally. The profile is stopped after main35488/engine27772 exited. Summary: `.local/native-smoke/task4-native-controller-summary.json`, SHA2564684d2e1f7996b477755216f7d6290b447cc6215b50ceaa4254f3c3980e1b651. Default smooth animation in a visible host, file-symlink execution after WinError1314, final release/clean-host/offline-network/accessibility/uninstall and later consumers remain explicitly unverified.

Task5 source is saved through7f02077973e65bc9687315053dca08638bff6968 from the clean M2 base619df709: six provider adapters, explicit profiles/capabilities, notebook consent, secret storage and usage/budget accounting. A pre-review correction makes shutdown drain catalog and embedding calls before resources close. Its covering security check passed23cases; strict mypy45 and Ruff passed, with the existing upstream warning retained. The complete two-commit source is undergoing its first independent review; live adapter validation remains pending. The worker recovered from a quota interruption after current account availability was observed, without consuming a reset or changing models. Root rechecked the original owned Ollama process and exact model digests at07:17Z without inference; see LOCAL_MODEL_TEST_ENVIRONMENT.md. Task6 remains undispatched. Root owns native fixtures, the authorized local model environment and controller documentation. Earlier milestone history and original failures follow.

Task2 round5 b132506 passed independent review and the actual native first-notebook flow: consent, authenticated engine startup, creation, selection, close and reopen. A further host restart recovered the same notebook. The real 900x528 panel capture was visually inspected. A further15-check native run proved two mounted main-window panels, trusted Enter creation synchronized across them, light/dark system theme propagation and fullZoom2 stacking without horizontal overflow. The12-check native Reader run then imported a synthetic two-page PDF into the test profile, opened the registered296x367Evidra section with an opaque connected frame and the current notebook, closed the reader and reopened the main workspace. Both final-window closes removed their exact owned engines. File-picker, complete keyboard/accessibility/localization and disable/uninstall acceptance remain pending.

Task3 source/scope implementation passed independent review after round1 at `24bc78c80819b1925ed069ff758be761254a8b46`. F1–F4 correct transport-aware pagination, exact child-content/current-observation scope, stable idempotent recovery after uncertain capture and the production Engine route allowlist. Original RED/GREEN outputs, generated-contract hashes and the independent verdict are retained in the Task3 report and re-review. The reviewed XPI is110097bytes, SHA256 `c1cffaaf20cbc26ee002001721160d8c22d7c7eb099185747e10463c8fb15423`, at `.local/task03/reviewed-package-20260906T024850Z/evidra-0.1.0.xpi`.

The exact reviewed source archive produced the preliminary M1 Windows engine,214files/67593760bytes, manifest SHA256 `90b791c28557bc46fbaf62317df9b863cef1ae6c343dfbba9d4d53e8c72a953a`. Its real process smoke passed authenticated notebook and initial snapshot/identity/source routes, migration, heartbeat-expiry exit0 and socket/receipt/database cleanup with only System32 on the child's PATH. Native Sources checks on these exact artifacts confirmed two-item selection/preview/capture, stale original metadata, persisted revocation after restart and native trash blocking all remaining historical access. The first helper timed out because it requested a read before its prior revoke action finished; its FAILED receipt remains intact. A separate11-check continuation passed. Both isolated hosts closed normally and their exact engines were absent afterward. Task4 implementation has started on BASE24bc78c, with registered documents, bounded asynchronous parser operations, evidence and lexical retrieval as its scope.

The additional native collection/filter check passed17observations on the same reviewed artifacts: two overlapping collections deduplicate Shared, the selected saved search joins the union, descendant opt-in adds only its child, inclusive2024 and AND-tag filters narrow the set, and explicit exclusion permits an empty immutable snapshot without expanding scope. Exact preview counts are3/4/5/2/1/0; receipt `task3-native-collections-corrected-receipt.json`. The first collection helper's FAILED receipt is retained: it waited on a nonexistent itemsView.initialized property. The correction uses the installed public waitForLoad API and reuses the same guarded synthetic fixtures. Main12972 closed normally03:21:02Z; its engine17324 was absent03:21:03Z.

Complete parser, providers, matrix/jobs/research, MCP, exports and distribution as recorded in the plan. NATIVE_VALIDATION_PLAN.md pins exact tested artifacts and isolated paths. The user explicitly authorized its isolated launch/install/preferences/API/DOM exception on2026-09-05. The personal profile remains outside native testing.

## Public project repository

The user requested `Evidra-zotero` on GitHub and corrected its visibility to public. The public repository https://github.com/Marcux777/Evidra-zotero now contains reviewed production24bc78c and controller evidence documentation at `9efa10c6fe0b78ffb03c79ea56203c0f2fa19dc7` on `main`; remote readback matched at03:04:35Z. Dirty Task4 files remained outside the pinned publication. The same repository hosts the separately authorized empty update feed, verified200/exact77bytes/SHA256 `901686d6fab93aba6f5b115b5002f7fa630af72c09a2d59402b15549c14bd96a`. No release binary or ignored local cache/database was published. Receipts: `.local/publication/first-publication.json` and `.local/publication/reviewed-source-20260906T030435Z.json`; scope: UPDATE_FEED_PROPOSAL.md.

# Evidra test report

Updated: 2026-09-07. Task 12 local distribution and bounded acceptance evidence are available. Native final regression and independent review remain pending; this is not complete-v1 acceptance. Earlier dated results below remain historical evidence.

## Task 12 final local evidence

The package was built from clean commit `a808abc4442e595012b24b1615292b0d62fe39dc`. `.local/task12/package-2/inputs-before.json` and `inputs-after.json` have identical file lists; their canonical input digest is `3d36b6a8e98fed7a7848e726a7dce460fab2030f7b949f8828ed4701c18097f4`. `source-inputs.zip` preserves the actual bytes, including Windows line endings. The later smoke-test header correction and final documentation do not modify the bundled runtime source.

| Check | Executed result | Evidence |
|---|---|---|
| `npm ci`, frozen `uv sync`, `npm run typecheck` | Passed | `.local/task12/check-final-1/commands.json` and `logs/` |
| Full `npm test` | 99 passed, 2 failed out of 101; both failures were stale manual-ready text expectations in `vertical.test.tsx` | `.local/task12/check-final-1/vitest.json` and raw logs |
| Focused UI correction | All 6 `vertical.test.tsx` cases passed after correcting the obsolete assertion; combined evidence covers all 101 cases, not a new all-green full run | `npm test -- apps/zotero/tests/vertical.test.tsx`, console result 2026-09-07 07:00 UTC |
| Full engine pytest | 281 passed, 4 failed, 1 skipped out of 286; failures hardcoded schema5 in the old migration test | `.local/task12/final-pytest.xml`; workflow `task12-remaining-checks-f5d2870c361b43fba8ae73c778c71a86/logs/pytest.stdout.log` |
| Focused migration correction | 4 passed, preserving old notebook/source data, pre-upgrade backup and rollback against the actual next schema | `.local/task12/fixed-migrations-pytest.xml`; combined evidence covers285 passed/1 skipped |
| Strict typing and lint | mypy passed102 source files after annotating generic transfer/history/SQLite returns; Ruff passed after import-order correction | Commands use the documented root configuration; original Ruff failure retained in `task12-remaining-checks-c9d3807e2eaf477c84607e859b6fb148` |
| Export typing/idempotency check | 2 existing roundtrip/cached-PDF tests passed; no new behavior test added for annotations | `.local/task12/fixed-export-typing-pytest.xml` |
| Generated contracts and XPI | Generation passed with no semantic Git diff; XPI build passed12 resource checks | `.local/task12/package-2/build-receipt.json` |
| Retained approved-cell regression | 3 passed across two CSV orientations and backup; nonexistent retained proposal still rejected | `test_exports.py::test_export_current_cell_survives_rejection_of_a_competing_proposal`; initial CSV2fail and backup1fail reproduced before fixes |
| Cache/UI focused work | Migration14→15, derived-only LRU, invalid settings/preview eviction; locale parity/UTF-8, both-locale named controls, matrix row keyboard virtualization | `test_cache_limits.py`, modified `test_documents.py`, `locale-accessibility.test.tsx`, `matrix-ui.test.tsx`; included in full suites above |
| Windows package |597 engine payload files; production verifier accepted exact file set/hashes; ZIP path/CRC checks passed | `.local/task12/package-2/package-report.json`; workflow `task12-windows-package-a000e871043e42c59f903ee7a98af286` |
| Packaged-process smoke |16 checks passed across two real frozen Windows sessions; child PATH contains only System32 | `.local/task12/package-smoke-2/report.json`, SHA `c335a1a94f5f8d36ba4720ae0ea79b0d22637723a8a0d2e381a8b28dab921349`; workflow `task12-packaged-smoke-2720defa629d466fb0ef41a3b03ba6eb` |

The packaged smoke exercises authenticated notebook/snapshot/search, exact original evidence, an approved matrix cell, a 15,769-byte JSON export, and frozen PDFium parsing of a real synthetic 1,055-byte/two-page PDF. Parsing took0.409s and its cache reuse0.0265s. A second session revalidated source availability and retained the approved value42. Both processes exited0 after the real30-second heartbeat expiry, removed their connection receipts and closed their sockets. The verifier rejected deliberately malformed manifest JSON and changed payload bytes; the disposable payload was restored and reverified. Session credentials were absent from the captured process arguments and engine logs. This is a developer Windows host with child PATH restricted, not a clean Windows-machine test or native Zotero smoke.

The full-check runner stops at the first failure and retains exact return codes and raw outputs. Successful checks were not unnecessarily repeated. Failures and their focused corrections above must be read together. One file-symlink case remains skipped because Windows returned WinError1314; no privilege change was made. Existing Starlette deprecation and React act-environment warnings are retained in raw logs and are not counted as test passes.

## Task 12 measured performance

`.local/task12/benchmark-3/report.json` (SHA `bb4613e9dd4fab8d19b6a3f0969d1df10e3e03c8c5053042f25ebf302ab1f521`) records Windows11 Home, AMD Ryzen7 7700 (8 reported logical processors), approximately31.2GiB RAM visible to Windows, current process ownership and GPU inventory. The production CPU path uses SQLite FTS5 and NumPy float32 cosine in blocks of64 vectors; no GPU or model is required. Hardware is bound by `.local/task12/hardware.json`. The run captures HEAD `af69b4a` plus its then-uncommitted actual input bytes; it is not relabeled as a later clean commit. Before/after input receipts match (file SHA `fa452577818b04d13b2ad30a5ed78afa28357230ea6025c4a1ee61fab798f488`). Root's independent arithmetic/input audit is `.local/task12/root-benchmark-binding.json`; later typing-only changes did not justify a rerun.

| Measurement | Result |
|---|---|
| Corpus |1,000 synthetic sources,50,000 short structured pages/chunks,6,567,500 original UTF-8 bytes; corpus SHA `e628162db43c099ccfc087415d8257cb1da4c5096e577544a7ba2e6a534c54cf` |
| Source sync / selection resolution / snapshot |0.117s /0.060s /0.087s |
| Production page persistence, normalization and FTS indexing |8.543s; this is structured fixture indexing, not parsing1,000 PDFs |
| Vectors |50,000 seeded random unit vectors,384 dimensions, float32, seed7; no semantic-quality claim |
| Warm retrieval |30 measured queries after3 warmups: lexical p95 0.346s, vector p95 0.934s, fusion p95 0.000059s; combined p95 1.265s, below2s on this corpus/host |
| Memory / index |PeakWorkingSet127,111,168B; peak committed pagefile373,391,360B; SQLite320,512,000B |
| Restart and current-source revalidation |1.085s; all50,000 chunks retained and40 scoped hits returned |
| Not measured |Query embedding, provider first/final token, native UI300ms progress goal, PDFium child peak memory and real-paper corpus speed |

Benchmark1 correctly rejected a read-only ScopeContext used by the fixture seeder for a commit; the runner now requests a server-created commit context. Benchmark2 completed retrieval but its restart assertion omitted the mandatory new-session source revalidation. Benchmark3 fixed the runner and passed its named invariants. The original failed reports remain in `.local/task12/benchmark-{1,2}/`; their first errors were observed in console, with a terse/empty assertion message in the old report. The final runner now writes full traceback to `logs/failure.log` on failure. No production scope rule was weakened and no model call occurred.

## Native failures and remaining acceptance

Task11 saved nine artifacts using real Windows file pickers and Zotero translators; `.local/native-smoke/task11-native-controller-summary.json` binds the native results and43 independent backup/bibliography checks. It also records the CSV state/proposal defect and valid-backup `INVALID_BACKUP` failure after rejecting a competing proposal. Source commits `b40c224` and `af69b4a` resolve those two defects. Read-only validation of the original unchanged `backup-with-pdfs.zip` now returns44 records/1PDF. Final native CSV/backup/mapping/evidence/UI regression is pending; the old native failures are not marked passed.

The three original Task9 screening/synthesis/audit runs each failed `GENERATION_INCOMPLETE` with qwen3:4b/context32768/output4096/temperature0/seed7/thinkfalse. No accepted artifact, retry or provider substitution resulted. These settings were not altered and those runs were not repeated. Task8 successful native extraction/cancellation, clean Windows, externally blocked-network smoke, complete native accessibility/uninstall, installed external MCP clients and paid-provider live tests remain separate unresolved gates. Ollama had actual earlier synthetic live generation/embedding smoke; LM Studio, OpenAI, Anthropic, Gemini and OpenAI-compatible adapters have controlled protocol coverage but no claimed live smoke here.

Package1 failed at optional MCP CLI discovery (`ModuleNotFoundError: typer`); the build spec now collects the120 SDK modules while excluding the unused optional CLI. Smoke1 failed because its missing-token probe also omitted the required client marker and was refused403 before the expected401. Smoke2 corrected that test request and passed; the product's authentication policy and package binaries were unchanged. Full original causal logs are retained beside the corresponding failed workflow receipts.

## Categories

- IMPLEMENTED: production behavior exists; this is not by itself verification.
- VERIFIED_WITH_FIXTURES: repeatable executed check against synthetic/authorized fixtures.
- VERIFIED_LIVE: the actual external application/provider/runtime was exercised.
- NOT_VERIFIED/BLOCKED: missing implementation, unrun check or blocked environment; include its reason.

## Executed environment checks

| Check | Command | Observed result |
|---|---|---|
| Local Zotero before update | `rtk py -3 C:\Users\marcu\plugins\zotero\skills\zotero\scripts\zotero.py start` then `status --json` | Version 9.0.6, API and Connector 200; incompatible with target. |
| Official installer | Official download URL; Windows Get-AuthenticodeSignature after explicit import of installed Security module | Authenticode Valid, Corporation for Digital Scholarship, SHA-256 `4b0f508ef28bff9a7b6cc2299eb18f2431d966d91a462ce29e5bc609d5e07036`. |
| Authorized update | Verified no zotero.exe process after user closed it; executed downloaded official installer with `/S` | Exit 0. No forced termination. |
| Zotero after update | Executable/registry version check, personal helper `start`, `status --json` | Version 10.0.1, schema 44, API and Connector 200. This verifies the host, not the future Evidra plugin. |
| JavaScript dependencies | `rtk npm ci --no-audit --no-fund`, `rtk npm ls --depth=0` | 126 packages installed, dependency tree valid. Transitive test dependency warning: deprecated whatwg-encoding@3.1.1. |
| Python dependencies | `rtk uv sync --project services/engine --frozen --python C:\Users\marcu\AppData\Roaming\uv\python\cpython-3.12.11-windows-x86_64-none\python.exe --no-python-downloads` | 63 packages installed in project .venv; no new interpreter installed. |
| Runtime/FTS5 | `rtk uv run --project services/engine --no-sync python -c <imports and CREATE VIRTUAL TABLE USING fts5>` | Python 3.12.11, SQLite 3.49.1, FTS5 available, FastAPI 0.141.1, Pydantic 2.13.5. |

Initial dependency resolution failure: `ERESOLVE` from TypeScript 7.0.2 versus openapi-typescript's `^5.x` peer. Corrected the manifest to verified TypeScript 5.9.3; lock-only resolution and npm ci then succeeded. No `--force` or `--legacy-peer-deps` used. Initial `py -3.12` registry entry pointed to an absent StabilityMatrix interpreter; uv's already-installed Python 3.12.11 was discovered and verified instead. Winget lookup failed; it did not perform the update.

## Behavioral tests

Task 1 implementation commit: `2759afbae9638e742bf96aa2174e26b2e3efbca8`. Independent review passed after the fix commit described below. No model call formed part of Task 1. A later authorized local model-environment smoke is documented below; paid APIs remain unused. The first authorized Zotero test profile was subsequently created, but its initial XPI was rejected before bootstrap as recorded below.

| Check | Exact final command | Observed result / full evidence |
|---|---|---|
| Auth, persistence, migrations, concurrency, request limits and Windows file boundary | `rtk proxy uv run --project services/engine --no-sync pytest services/engine/tests/test_runtime_notebooks.py -q` | 21 passed, 1 warning, 5.88s, exit 0; `.local/task-1/green-0.log`. |
| Python lint | `rtk proxy uv run --project services/engine --no-sync ruff check services/engine/src/evidra services/engine/tests/test_runtime_notebooks.py` | All checks passed; `.local/task-1/green-1.log`. |
| Strict types | `rtk proxy uv run --project services/engine --no-sync mypy --strict services/engine/src/evidra` | No issues in 16 source files; `.local/task-1/green-2.log`. |
| Wheel build/package data | `rtk proxy uv build services/engine --wheel --offline --out-dir .local/task-1/dist` | Exit 0; initial SQL migration is in the ZIP; `.local/task-1/green-3.log`, `wheel-receipt.json`. |
| Real process restart and expiration | `rtk proxy uv run --project services/engine --no-sync python .local/task-1/smoke.py` | Two sessions, exit 0 each, same persisted notebook; handshake consumed, receipt removed, socket closed; `.local/task-1/smoke-receipt.json`. |
| Real Windows path substitution | `rtk proxy uv run --project services/engine --no-sync python .local/task-1/path_security.py` | Hardlink and junction ancestor rejected as UNSAFE_PATH; `.local/task-1/path-security-receipt.json`. |

RED evidence: `.local/task-1/red-api.log` contains 15 expected missing-API failures. The first outer PowerShell wrapper did not propagate the test exit code, so that wrapper's success is not a RED claim. Subsequent direct focused runs reproduced missing CLI, unsanitized storage failure, absent receipt cleanup and closed-database backup errors before their fixes. Exact commands, error causes and invariant mappings are preserved in `.superpowers/sdd/IMPLEMENTATION_PLAN/task-1-report.md`.

Visible upstream warning: `starlette/testclient.py:53: DeprecationWarning: The anyio.abc.BlockingPortal alias is deprecated, use anyio.from_thread.BlockingPortal instead.` No blanket suppression or dependency switch was added. The initial ACL command failed because Get-Acl could not autoload the installed Security module; original stderr is `.local/task-1/acl-error.log`. Explicit import of the verified installed module fixed that cause; an intervening parser error remains in `acl-error-explicit-import.log`.

Wheel SHA-256: `1e11fdf3707e7fd234f99958076bd95b6a339b2917cf9e6d39306e3beb2d33ce`. Synthetic smoke database SHA-256: `1165d781326586a17f84bca1bae3914c5cb5722d14b420f9bee50e57a1e15d49`; SQLite integrity_check returned `ok`. These identify Task 1 evidence, not final release artifacts.

### Task 1 review fixes

Independent review found that delayed request-body intake could outlive heartbeat authorization and that the CLI discarded causal startup diagnostics. Fix commit `a47c06e3870b8e0715dfff3a68b69fd449c36681` rechecks liveness immediately before dispatch and emits allowlisted structured diagnostic causes. Raw validation inputs, arbitrary messages, process arguments, environment and personal paths remain excluded. The original exception chain remains internally intact until the process boundary.

The delayed-body check first returned 200 at 31 seconds (RED), then correctly returned 503 (GREEN). Three actual CLI processes tested a missing private handshake file, a malformed protected handshake and an occupied exclusive socket; every process exited 1, kept stdout empty, and returned distinguishable safe causal diagnostics on stderr. Tests assert that session/provider-secret markers and fixture paths are absent. Original ACL development logs remain retained.

Final covering command: `rtk proxy uv run --project services/engine --no-sync pytest services/engine/tests/test_runtime_notebooks.py -k 'auth_boundary or cli_failure or windows_handshake' -q`: **18 passed, 7 deselected, 1 upstream warning, 8.22s**, exit 0. Changed-file Ruff and strict mypy also passed. Full receipts: `.local/task-1/fix1-final-0.log` through `fix1-final-2.log`; RED/GREEN outputs are `fix1-f1-red.log`, `fix1-f1-green.log`, `fix1-f2-red.log`, `fix1-f2-green.log`. The scoped independent re-review confirmed both findings addressed with no new Critical/Important breakage. The earlier wheel/process-success receipts identify the initial implementation commit and were not rerun or misattributed to this fix.

## Preliminary packaged M0 runtime

While Task 2 was implemented, root built a disposable PyInstaller onedir from the reviewed Task 1 runtime. This is a development package for native test preparation, not the final v1 distribution. Build script: `.local/build-native-engine.py`; full log: `.local/native-smoke/m0-engine-20260905T213413Z/build.log`. PyInstaller 6.22.2 completed its build at log elapsed 20.083s. Its 213 runtime files total 67,557,816 bytes.

The initial preparation then failed with Pydantic `files.203.path` rejecting the real dependency filename `_internal/setuptools/_vendor/jaraco/text/Lorem ipsum.txt`. Task 2 extended its existing manifest regression check, observed RED, allowed interior spaces while retaining unsafe-segment rejection, regenerated contracts and passed the covering checks. Root resumed only manifest emission, preserving the successful build and original error in `manifest-failure.log`. The subsequent harness initially failed before launching the engine because a plain environment dictionary used `SystemRoot` instead of its uppercase key `SYSTEMROOT`; `smoke-c08f625d9091/harness-failure.log` retains that cause, and the unused own synthetic handshake was removed.

Corrected command: `rtk uv run --project services/engine --no-sync python .local/native-smoke/check-packaged-engine.py .local/native-smoke/m0-engine-20260905T213413Z/dist/evidra-engine`. Exit 0. The child PATH was only `C:\WINDOWS\System32`, with Python/Node-specific environment variables removed. The actual frozen process (PID 4120, port 50881) passed minimal health, denied unauthenticated status, created/idempotently read one notebook, expired its heartbeat and exited 0. Handshake consumed, receipt removed, socket closed, SQLite integrity_check `ok`; output logs were checked for the generated secret.

Receipt: `.local/native-smoke/m0-engine-20260905T213413Z/smoke-be843d60f511/smoke-receipt.json`. Manifest SHA-256 `96906d631de3e6102b94c59083a3b638ed84bfb5e97e565b202893817b96294f`; executable SHA-256 `53f79523fdddc24592dce73770fc97a78033222d8fb5b256596780cfc16c974a`. No Zotero process or provider was invoked. A host without Python/Node installed, native plugin lifecycle and all later v1 services remain unverified by this preliminary check.

Compute preflight: Windows 11 Home x64, AMD Ryzen 7 7700, 8 cores/8 logical processors, 32,679,372 KiB visible RAM and 13,523,256 KiB free. An initial transient CPU load sample was 79%; a follow-up process counter showed 667 idle out of 793 aggregate units and no sustained Python/PyInstaller job. This used the installed default CPU build path without changing fidelity or claiming GPU acceleration; no other process was terminated. Future benchmarks require a fresh ownership/availability check.

## Permanent test-change accounting

Task 2 initial commit `cae92f6ba3b982e1a685fa9bda45a537fba39a5f`: 10 new Vitest tests in three files; no existing tests modified/removed. The task report maps them to message capability checks, Markdown isolation, diagnostic redaction, React notebook flow, real payload integrity, controller consent/lifetime, concurrent startup/shutdown, temporary handshake failure, native-dialog cleanup and cloned-profile identity. Final initial commands `rtk npm run typecheck`, `rtk npm test`, targeted Ruff and `rtk npm run build:plugin` exited0. Receipts `.local/task-2/final-reviewed-check-{0,1,2}.log` and `.local/task-2/dist/build-receipt.json`. Initial XPI: 87,667 bytes, SHA256 `3e96cc93501cff3fc21b354df92b5a522d73491e631731d24451ffa1991a7024`. This artifact is under review and will change after fixes.

Task 2's production adapter ran against real Windows ACL commands and the preliminary frozen engine using a Node replacement for Gecko host APIs. Receipt `.local/task-2/adapter-1788645412263/receipt.json` confirms creation/readback of synthetic notebook `f6d462d5-40ef-4a38-9fb2-44aaadc6fc7e` and owned PID39884 termination. It records forced Windows termination; it does not prove graceful cleanup or native Zotero execution. Independent review found stale consent across fingerprint changes, stale notebook lists across mounted panels, and two paths discarding causes. Fix evidence follows; actual native acceptance remains unverified.

### Task 2 review fixes

Fix commit `4240646b318ad2b2a4306f85c4f00b6bbb549b35` binds executable consent to the acknowledged fingerprint, refreshes current notebook pages with the existing ten-second poll while rejecting stale responses, and retains sanitized structural causes for saved-package and heartbeat errors. Privileged startup re-verifies the payload and refuses a fingerprint race before any launch. Unsaved controls survive cross-panel refresh. The cross-panel convergence bound is a poll interval plus request latency, with polls deferred during local operations.

Five new cases cover those material findings; two existing cases were updated for the required diagnostic reporter, eight existing cases ran unchanged, and none were removed. RED/GREEN receipts are `.local/task-2/review-f1{red,green}.log`, `review-f2{red,green}.log`, `review-f3{red0,green0,red1,green1}.log`; the appended task report gives exact commands and assertions. Final covering `rtk npm run typecheck` and `rtk npm test` passed: 15 tests in four files; logs `review-final-check-{0,1}.log`. XPI build and whitespace check also exited0. Python/schema files were unchanged, so their earlier checks were not repeated.

Rebuilt XPI: 88,142 bytes, SHA256 `f71bb79ffb2e510845c9799171d3b19f92cde448ada8e15c10c3021f5be61166`; the preparation receipt carries the same hash. The updated controlled adapter receipt `.local/task-2/adapter-1788646692511/receipt.json` records notebook `76150d3d-2ecf-4015-8521-35b739565134` and PID624 termination (SIGTERM mapped to exit -9). It remains controlled-adapter evidence, not native Zotero or graceful termination evidence. Independent scoped re-review confirmed F1–F3 ADDRESSED with no new Critical/Important breakage; report `.superpowers/sdd/IMPLEMENTATION_PLAN/task-2-rereview1.md`.

Setup commit added no permanent tests: only approved specifications, configuration/lockfiles and a nonbehavioral package initializer changed. Subsequent behavioral/security/persistence checks are tracked by task reports and the acceptance matrix. Extra checks above the ordinary zero-or-one default must each name a distinct material invariant.

Task 1: 1 added test file, 9 test functions, 21 collected/executed cases; 0 pre-existing tests modified or removed. The report maps each function to a separate material invariant: session authorization, durable idempotency/profile isolation, bounded/redacted input, transactional sanitized storage errors, migration lifecycle, concurrent creation, credential file ACL/claim, exclusive listener binding, and safe receipt cleanup.

Task 1 fix round 1: one existing function gained one delayed-body case; one new function has three real CLI failure cases; no tests removed. The file now contains 10 functions and 25 cases. Eighteen covering cases were executed after the fix; seven unrelated persistence/lifecycle cases retain their earlier execution evidence. The final full-project check will report its own count at its exact revision.

## First authorized native-profile attempt and required update metadata

The user explicitly authorized the isolated profile/data/preferences/native API/DOM test in NATIVE_VALIDATION_PLAN.md. `prepare-task2-profile.py` verified the exact f71bb XPI and preliminary engine manifest, created only the named test profile/data paths, and staged a separate test-only probe. `launch-task2-profile.ps1` launched Zotero10.0.1 with both explicit paths at22:32:28Z. The launcher39700 handed off to main process37152; ownership was later verified from executable, exact profile/data command line and parent PID. The existing personal-instance PIDs remained present.

Result: **FAILED before plugin bootstrap**. Zotero removed the two invalid XPIs; the test profile's extensions.json contained `addons:[]`, and the probe never produced a native-flow receipt. Installed `omni.ja/modules/Extension.sys.mjs:1878-1879` requires applications.zotero.update_url, omitted in the reviewed XPI, with `XPIInstall.sys.mjs:481-486` propagating `Extension is invalid` and additionalErrors. Receipt `.local/native-smoke/task2-install-failure.json` distinguishes that sufficient source-proven rejection cause from the unavailable actual console exception. Full host stdout/stderr remain retained, including upstream missing-locale/resource/shader warnings. No notebook creation, reader, CSP, focus or cleanup criterion is marked passed by this attempt.

The first normal-close ownership gate stopped with exit1 before issuing a close, because recorded launcher39700 had already exited. The initial read-only observation identified child37152 with the exact approved command line; it is preserved with its provenance in `task2-owner-initial-observation.json`. The old generic snapshot was subsequently overwritten by a later no-owner inspection, and the inspector now writes unique timestamps. A targeted normal CloseMainWindow request was accepted and the process exited, with no forced termination; `.local/native-smoke/task2-close-{initial-failure.log,receipt.json}` preserve both outcomes.

The embedded data-URL proposal passed the installed pure update-manifest parser but failed the installed `providesUpdatesSecurely` getter; default update-security checking makes that addon unusable. `.local/task-2/f5/source-check.json` and the appended task report preserve the inspected source hashes and exact gate. No production edit, disabled security setting or passing native result came from that rejected proposal.

The user subsequently authorized an empty HTTPS feed and public project publication. The public repository main ref matched pushed commit344fbc1, and the feed returned200/exact77bytes/SHA256 `901686d6fab93aba6f5b115b5002f7fa630af72c09a2d59402b15549c14bd96a`, zero update downloads. Publication receipt: `.local/publication/first-publication.json`.

### Actual native admission and transport failure after fix2

Task2 fix2 at `f76e8a480cf4fe94892bc2d147fdbadf1d53f27e` passed scoped review. Its isolated committed-source build yields XPI SHA256 `f83d58b47dcd0c1a79cc290b7b4ab6e67c0932f7ff46a7b672e8d43fd986d53f`; all11 non-manifest resources match the previous artifact, and all16 then-existing Task3 partial files remained unchanged. The failed archive/license-link and CRLF byte-comparison preparations remain under `.local/task-2/f5/`; the final build is `https-20260905T225535Z`.

At23:03Z, the authorized test launch handed launcher20456 to verified main10448. Native receipt `.local/native-smoke/task2-native-fix2-receipt.json` confirms exact isolated profile/data, active Evidra0.1.0 and actual Gecko AddonUpdateChecker accepting the approved HTTPS feed with zero updates. It then failed a test-helper menu lookup, not a production registration assertion: installed menuManager.js creates menu elements lazily on popupshowing and assigns data-l10n-id instead of the registration ID. The test instance accepted normal close and exited; `task2-fix2-close-receipt.json`.

Root corrected only that helper, preserving its previous XPI and receipt. The next launch at23:08Z handed launcher32868 to main7856 with the same exact authorized paths and unchanged production XPI. Receipt `task2-native-menu-corrected-receipt.json` confirms the native menu opens the workspace dialog, the iframe retains sandbox=allow-scripts, and the actual React page renders. The flow then **FAILED** with visible UI error `Permission denied to access property "postMessage"`. No onboarding/consent/engine/notebook flow passed. Native console also records a saved-package diagnostic `{operation:native_bridge,causes:[{type:Error,code:OPERATION_FAILED,system_code:0}]}`, whose cause still needs investigation. Task2 round3 is reopened for these actual native boundaries; Task3 is paused with its partial files preserved. These test-helper changes add no permanent product tests.

## Authorized live local model environment

After the user's explicit installation authorization, root installed official portable Ollama0.33.3 plus qwen3:4b and qwen3-embedding:0.6b. Archive verification, hardware/ownership preflight, local-only startup, actual model digests and full request/response receipts are recorded in LOCAL_MODEL_TEST_ENVIRONMENT.md and `.local/ollama/`. The server binds only127.0.0.1:11434 with cloud disabled.

Executed command: `rtk uv run --project services/engine --no-sync python .local/ollama/smoke-local.py`, exit0 at2026-09-05T22:04:37Z. One preload, one structured streaming generation and one three-text embedding batch succeeded. Both models report fullGPU placement. The generation returned the expected synthetic87.5% value in31chunks; embeddings are finite normalized3x1024. This is a raw provider-service smoke, not an Evidra adapter/retrieval test. No new permanent tests were added for installing that test environment. Future adapter/code changes need their own regression and live integration evidence. LMStudio, paid-provider and external-network-blocked acceptance remain unverified.

## Native diagnostic observations for Task2 round3

At23:18Z, the separate fixed test-only helper used the unchanged reviewed f83d production XPI and verified the same isolated profile/data. Receipt `.local/native-smoke/task2-native-transport-package-diagnostic-bounded.json` records actual Gecko behavior: a self-posted content request reaches the privileged listener attached to that contentWindow with exact child source, origin `null` and isTrusted=true. A chrome-to-child reply reaches the content with source=null, empty origin and isTrusted=true. The test iframe has an opaque principal; calling parent.postMessage reproduces the permission error. This is a transport experiment, not a passing product flow.

The same helper bundled the reviewed snapshot's production nativeEngine and called only verify on the pinned preliminary payload, with real IOUtils/Subprocess delegates instrumented for operation attribution. Verification failed before any subprocess or secret creation: `OperationError: PathUtils.join: Could not append to path: NS_ERROR_FILE_UNRECOGNIZED_PATH`, result2152923168/code0, stack at native command's PathUtils.join(system, executable). The executable value includes multiple path components. Full exception/stack is retained in the receipt. The production structural diagnostic had omitted OperationError and therefore reported OPERATION_FAILED; that category needs preservation in the correction.

The preceding test helper stalled awaiting an unbounded frame load event; its RUNNING receipt is retained alongside `task2-diagnostic-harness-failure.json`, and that instance closed normally. The corrected experiment uses bounded content-script readiness and independently captures package failure. Its test-only helper SHA256 is `02b0044ee8d54d0fda5583710a2877b5e23cee8a492a1abd8bd65c1bc0ac10d9`; no production artifact changed. Main process39788 belongs to launcher38480 in `task2-diagnostic-bounded-launch-receipt.json`.

### Task2 round3 implementation and covering checks

Commit `a06637f0e3189dd47c4830ec9b0b91cefc4ae0b4` changes seven paths (+168/-48) for the measured transport/path failures and structural diagnostics. The UI keeps its opaque iframe/CSP, queues requests until native readiness and self-posts to its own window; exact native source/origin/trust and envelope checks distinguish replies. The native listener checks the actual child and removes its listeners during cleanup. Fixed executable paths use separate PathUtils components. Diagnostics retain documented DOMException names and inherited numeric result without raw messages.

Two new boundary cases and one extended existing diagnostic case reproduced three RED failures, then passed. The transport case also reproduced a JSON-null TypeError and passed after its guard. Final snapshot commands `rtk npm run typecheck` and `rtk npm test` passed: **17/17 cases in5 files**, no suite warnings;14 existing cases ran unchanged,0 removed. Logs/commands are `.local/task-2/f6-20260905T232700Z/{test-receipt,final-validation}.json`. The PathUtils check uses the production verifier with the measured host component contract; it is not native engine execution evidence.

The isolated snapshot XPI is 88,614 bytes/SHA256 `92e8cc98c89c2de1dfc196f33b52a084ab4388a4250fa9e0ea8327736b599c0c`; only native.js/ui.js changed, with all 10 other resources including the manifest identical. postapply-receipt.json verifies 25 protected Task3 files byte-identical and the 26th restored exactly after reversing only the approved mount region. commit-receipt.json verifies the commit matches the tested snapshot. A caught CRLF conversion during git apply was corrected from the saved original bytes; failed preparation attempts and module-autoload diagnostics remain retained. Independent scoped re-review confirmed F6/F7 addressed with no new breakage; report task-2-rereview3.md. Actual product native flow remains pending.

### Actual native fix3 failure and captured document-load diagnosis

The actual fix3 run at23:40Z used the reviewed a06637f artifact and verified isolated paths. Receipt `task2-native-fix3-receipt.json` reports FAILED: onboarding did not appear within30seconds; React remained loading. The previous parent.postMessage denial was absent. Controlled tests and source review therefore did not establish native product acceptance. The exact owned main11780, launched by11212, accepted normal close and exited; `task2-fix3-close-receipt.json`.

A separate test-only observer then watched the unchanged production artifact without injecting readiness or replacing its listeners. `task2-native-load-diagnostic.json` records the iframe's real load event at66ms: capture phase1, trusted, target and originalTarget equal to the owned contentDocument, expected chrome UI URI, complete readiness. The event target was not the iframe. Noncapture load observers received no event. An earlier link load and interactive DOMContentLoaded were recorded separately. This establishes why the original noncapture, iframe-target listener could never start the channel on this host.

Diagnostic helper SHA256 `40b9e7aa72e0c97b78668b9188e68faa6c48a2d7e5829903cdb1cfb7a7b49062`; production SHA256 remained92e8cc98. Launcher32376 handed off to verified main2668, which accepted normal close and exited at23:50:50Z. Preparation, launch and normal-close receipts have prefix `task2-load-diagnostic-`. No engine was started by this experiment. Task2 round4 corrects only this boundary and its existing regression; Task3's27 partial paths remain preserved during the correction.

Round4 commit `652c3daabf32c682514b59be2f78ccf72f856d11` changes only the native mount and its existing boundary test. The listener captures the exact trusted/complete owned UI document at the fixed expected URI, ignores unrelated loads, and removes its handler with matching capture semantics. Zero permanent cases/files added, one case modified, none removed, one unchanged case executed. Focused Vitest passed2/2; typecheck and build exited0. Exact RED/GREEN and command outputs are `.local/task-2/f8-20260905T235200Z/{red-clean-command,green-command,typecheck-command,build-command}.json`.

New XPI: 88,687bytes, SHA256 `0abf8fb68cd864479d6b9a3c45ad748c48d1bb4e87105a149291ea3dbd3a827e`. Only native.js differs from92e8; all11other resources are byte-identical. An initial patch application failed because its context crossed into Task3 shutdown and the test's working copy used CRLF. The worker incorrectly continued to a second atomic failed worktree apply before staging the valid snapshot patch; these failures are retained in apply-failures.json. The corrected bounded application reconstructed only the approved mount from original bytes and preserved test CRLF. postapply-receipt.json and commit-receipt.json prove27Task3paths preserved and committed content equal to the tested snapshot. Independent scoped review confirmed F8 addressed with no new breakage; task-2-rereview4.md. Actual native execution has a separate evidence gate.

Actual fix4 launch at2026-09-06T00:05:45Z (still September5 locally) handed launcher27496 to verified main2628 in the approved test profile/data. Receipt task2-native-fix4-receipt.json confirms native onboarding, opaque principal, Portuguese locale, disabled execution before consent and actual Gecko startup of the authenticated frozen engine. The new synthetic engine data directory is `C:/Users/marcu/AppData/Local/Evidra/profiles/3aeaa45c-185d-4eca-ba11-3764a388ac9e`. It then FAILED after30seconds waiting for persisted notebook selection; the UI remained connected with no notebooks or visible error. Input/form event delivery requires diagnosis before assigning that failure to product code. No notebook/reopen/cleanup acceptance is inferred from engine startup.

### Native form and content-crypto findings, round5

The unchanged-artifact form observer `task2-native-form-diagnostic.json` recorded a trusted button click but no submit/bridge request. Its first input helper emitted an untrusted event that did not update React. The corrected helper used the focus-verified child window's nsITextInputProcessor. `task2-native-trusted-form-diagnostic.json` proves trusted input at capture/bubble, matching React value/attribute and an enabled trusted button click; no submit or bridge request followed. The HTML Standard's submission algorithm returns at the sandboxed-forms flag before submit dispatch. This establishes production finding F9 independently of the corrected helper problem. Both test instances closed normally; unique engine observations contain no surviving owned engine.

The fixed content-script experiment `task2-native-crypto-diagnostic.json` then established F10: the actual opaque content realm is not a secure context, randomUUID is undefined and calling it throws TypeError, but getRandomValues returns32bytes. Reading the same frame through privileged Xrays reports randomUUID as a function and is not reliable evidence of content capability. The production artifact remained0abf8fb6; no engine or credential was created by this experiment. Launcher12732 handed off to main21836, which closed normally at2026-09-06T00:29:12Z. The diagnostic helper SHA256 is `fc5b9d5a8cb4af018ba907c3015bba15e8619c595e7e924444bd20b7c567f03f`.

Task3's independently executable domain/staging slice was committed at `09d0bfaeaed85b3dfef6fbd25a6b2c7450c8e5ff`:33focused cases passed, Ruff/mypy22files/typecheck and a real SHA256 contract-stability check passed. Receipt `.local/task03/backend-slice-receipt.json` preserves commands, warnings and failed PowerShell hash attempts, whose invalid comparison was discarded. Preview requests derive exact candidate IDs/versions from a current-session notebook stage and preserve the64KiB body limit. Task3's remaining native resolver/UI work and complete independent task review are pending; its9residual paths remain protected while Task2 round5 is applied.

Round5 commit `b132506ffa64376fa70a00ee1fca71fbd90359e0` changes App and two existing tests (+84/-24). F9's click/Enter command rows and synchronization check passed7/7; after F10, the two affected rows passed with randomUUID absent and real getRandomValues available,4unrelated cases skipped. Typecheck and build passed. Zero new test files/standalone declarations; one existing declaration gained a second parameter row, and one synchronization declaration changed activation to click. The same rows verify validation/focus, IME/repeat/modifier rejection, busy draft freeze, one outgoing command, visible failure and stable64hex key on explicit retry. Covering receipts: `.local/task-2/f9-20260906T001916Z/` and `.local/task-2/f9-f10-20260906T002912Z/`.

Final XPI is88,808bytes/SHA256 `a1e957b8e053b64e0e6c3c9c1b62a1c4b41e4c63822456ac2677cad77eb9ffc7`; onlyui.js differs from0abf, all11other resources match. The commit exactly matches the tested snapshot, the index is empty,8Task3raw hashes match and exact reversal of the owned vertical-test region restores the9th original file. A failed global-diff guard had included unrelated root docs and changed Git index hashes; a redundant diagnostic guard repeated that ownership error. Both tracebacks are retained, the comparison is now scoped to the9protected paths, and no production patch was reapplied. Independent scoped review confirmed F9/F10 addressed with no new breakage; `task-2-rereview5.md`.

At2026-09-06T00:45:47Z, launcher17300 handed off to main34900 in the guarded test profile. `task2-native-fix5-receipt.json` confirms9native checks, including real authenticated engine startup and React creation/selection of notebook `cd724383-ad27-4cde-b137-ba97e477dd92` at00:45:53Z. It then FAILED waiting for workspace reopen. Installed `app/omni.ja` resource `chrome/content/zotero/xpcom/pluginAPI/menuManager.js:652-669` registers command with once:true and removes it on popuphidden; the helper incorrectly reused that old command without reopening Tools. This is a distinct helper failure after successful production creation, not a passing complete native run. No capture was produced. The same production artifact is retained for a helper-corrected test. Main34900 closed normally at00:48:45Z; engine32056, previously owned by34900 with the exact preliminary payload path, is absent in the subsequent unique process observation.

### Actual initial native flow and inspected panel

The helper-corrected launch at00:52:47Z used unchanged productiona1e957b8 and test probe SHA256 `4ecf3345f9f529af9dd20b9360c5b236f6aa7d741503ab1ecc1fb7a78c89de34`. Launcher16188 handed off to main23016. `task2-native-reopen-corrected-receipt.json` is **PASS_INITIAL_NATIVE_VERTICAL** with10checks: exact isolated paths, addon/update metadata, opaque iframe, Portuguese, consent, actual authenticated engine startup, real trusted-input creation and changed selection ID, panel close and reopened notebook readback. The new notebook is `0fea6a3a-e01f-41ea-8a45-fb95da11e8cc`; its distinct name prevents mistaking the prior selected notebook for a successful create. Main23016 closed normally at00:56:47Z and its exact owned engine40724 was absent immediately afterward.

The optional immediate post-reopen capture produced only10x10pixels/81bytes. Root inspected the image, rejected it as visual evidence and saved `task2-native-capture-assessment.json`; the earlier capture receipt's PASS meant successful image I/O only. No full-layout claim is based on that image. The original image and receipt remain unchanged.

A dedicated unchanged-product layout/readback check at00:57:13Z launched11244→39840 with probe SHA256 `c148427d7a0ea7b579c21dd833a995aa8c63be6e188a3f2349a0f6784818e214`. `task2-native-layout-diagnostic.json` confirms the previous notebook survived a host restart; actual host viewport1000x600 and iframe900x528 were stable before/after layout reads and500ms later. Installed Gecko drawSnapshot produced the actual900x528panel image,36,069bytes, SHA256 `7558ab617cbb23bd88c998c7fdc1c1acd425d77ffbb9d3f2dcb6f5ad11020de3`, at `.local/native-smoke/task2-native-layout-workspace.png`. Root visually inspected its dark Portuguese UI, notebook list, selected notebook summary and creation controls. This is software rendering of the native UI, not a claim about GPU compositor capture or complete accessibility. Main39840 closed normally at00:59:59Z; owned engine32844 was absent afterward. The personal seven-process tree was present before every launch. No new permanent product test was added by these disposable native checks.

Remaining native acceptance: actual OS file-picker interaction, reader and multiple windows, complete keyboard/focus/locales/themes/zoom, and disable/uninstall cleanup. These stay separate from the now-passed initial notebook flow and the eventual full product acceptance.

### Task3 complete implementation submitted for independent review

The three source/scope commits are `09d0bfaeaed85b3dfef6fbd25a6b2c7450c8e5ff`, `d1c3c548e691bfc9e82573ac90401b241b7e89a1` and `470ba5a64759643b7505963eec90b1f2fbbdfb0d`. The full report is `.superpowers/sdd/IMPLEMENTATION_PLAN/task-3-report.md`; commands, RED/GREEN output and hashes are in `.local/task03/{backend-slice-receipt,native-slice-receipt,final-receipt}.json`. Final runs: 28 Vitest cases in seven files; 33 Python cases with the existing AnyIO deprecation warning; typecheck, Ruff and strict mypy on22source files passed. All eight generated-contract SHA256 values matched the successful before/after generation receipt. The report maps each new/modified check to its authorization, identity, migration, staging, concurrency or UI invariant. Controlled native API fixtures are not a real-host pass.

Self-review reproduced a preflight invalidation failure that stopped the operation but left the engine running. The parameterized existing native-boundary check observed RED, then GREEN after the shared latched failure barrier stopped the owned engine and retained the original cause. No separate duplicate test file was introduced for that fix. Independent review of the complete original-BASE range is still in progress; these checks do not pre-approve unresolved integration behavior.

Root packaged the exact clean470ba5a revision with `rtk npm run build:plugin`; command exit0 and before/after Git identity/cleanliness are recorded in `.local/task03/package-20260906T012829Z/receipt.json`. Previous root dist artifacts were preserved in that run's `prior-dist`. New XPI:108,184bytes, SHA256 `d0353d99985f294de714c52252e5cf8b8fa958ad5886bffb72ea156a846e4237`. It is awaiting independent review and native execution; no Task3 addon was installed by this build.

Independent review subsequently returned spec FAIL / quality Needs fixes, with three Important findings in `.superpowers/sdd/IMPLEMENTATION_PLAN/task-3-review.md`. Round1 is correcting transport-aware preview pagination, exact child-content revalidation and explicit idempotent recovery after uncertain capture. F1's focused RED traversed the actual UI transport:101 valid sources with10000-character titles produced1063612characters and BRIDGE_TIMEOUT. The source gate remains closed; no later parser implementation has started. The reviewer's auxiliary generated-file summary failed with a Python regex PatternError; it supplied no new verification, and the original successful eight-hash generation receipt remains the evidence.

### Actual two-window, keyboard, theme and zoom check

At01:46:13Z, the same reviewed Task2 XPIa1e957b8 and preliminary M0 engine launched in the authorized profile. Probe0.1.14 SHA256 `c5282d66077220c1cc8dea8a8c529938c8870efc93450bdbe18a00d5589fa514`; launcher18924 handed off to main8468. Personal helper start/status were ready10.0.1/API200/Connector200, and the seven existing personal PIDs were recorded before launch. An initial read-only WMI observation failed from shell quoting and was corrected using a fixed script before staging; its original exception remains in `task2-multiwindow-preflight-shell-failure.txt`.

`task2-native-multiwindow-receipt.json` reports **PASS_NATIVE_MULTIWINDOW_KEYBOARD_THEME_ZOOM**,15checks completed01:46:58Z. Two distinct main windows owned distinct opaque React frames. Trusted nsITextInputProcessor Enter created notebook `1a792778-2055-4658-9f71-3535e9ebbf3e`; both mounted panels showed its list entry and selected heading. Changing only this test profile's native theme preference yielded light rgb(246,247,248) and dark rgb(24,32,39), with matching media queries in both frames. fullZoom2 gave logical viewport450x264, a single441.5px column, main below aside and scrollWidth=clientWidth442. The top-of-panel capture450x264/12220bytes was inspected; SHA256 `75f16944bab7420d172d1d3729bc6e4fb3c7d62370c9414faaaa2c82960052b6`. Main content continues below the viewport via vertical scrolling; this is not a full-page screenshot or full accessibility audit.

The second native window closed while the first panel stayed present, with connected status and no visible error through the next polling interval. Separate process observation confirmed exactly one engine11760 belonging to main8468 at the pinned executable path. Normal final-window close at01:47:42Z succeeded; `task2-multiwindow-engines-after-close.json` at01:47:43Z contains no matching engine. The helper restored fullZoom1 and the original absence of a user theme preference. No production code or permanent tests changed during this check; reader, OS picker, full keyboard/localization/accessibility and disable/uninstall remain unverified.

### Actual native Reader section

At02:14:03Z, unchanged reviewedb132506/XPIa1e957b8 and the preliminary M0 engine launched with probe0.1.15 SHA256 `354f4cb24584f93026414caa65ea1f9d7ce34455d0695a597e3c2e32d6ff0d17`. Personal helper start/status and a fresh no-test-owner preflight passed; launcher39748 handed off to exact owned main30652. `task2-native-reader-receipt.json` is **PASS_NATIVE_READER_SECTION**,12checks completed02:14:09Z. The helper created synthetic parentB5CXTHAU and imported PDFMDP6XFCU only under the guarded test-data storage path. The original two-page fixture has1627bytes/SHA256 `0694d0222f18e410969374921d26286c5646607ca4e41d1b853b285ad51f8f3f`.

The actual reader tab owned its item-details context and registered namespaced Evidra section. The section was open and visible, with a296x367frame, sandbox=allow-scripts, a null principal, connected engine and persisted notebook1a792778. Closing that reader removed its context and still allowed opening the main workspace with the same notebook. Separate process observation recorded exactly engine33140 owned by30652; normal host close at02:14:51Z succeeded, and the02:14:52Zafter-close observation contains no matching engine. No product code or permanent tests changed. This verifies the reader section lifecycle, not future evidence/quote positioning, the OS picker, full accessibility/localization or disable/uninstall.

Fixture authoring initially raised `TypeError: 'PdfPage' object does not support the context manager protocol` before creating output. Installed PDFium helpers confirm only PdfDocument offers that protocol; explicit finally/close corrected the fixture script. The same PDFium then authored/reopened both exact original strings. Original cause remains in `reader-fixture-initial-failure.txt`; this is a helper failure and does not count as Evidra parser verification.

### Task3 review corrections and frozen M1 artifact

Round1 commits c250264(F1),1b42b2b(F2),5e092f5(F3),24bc78c(F4) passed independent scoped re-review: all four addressed, no new Critical/Important breakage. Full report `.superpowers/sdd/IMPLEMENTATION_PLAN/task-3-report.md` and verdict `task-3-rereview1.md` retain exact covering commands/outputs. F1 verifies paginated real transport below the1million-character envelope; F2's11Python and11native-fixture cases cover current metadata/per-content observations, migration3 and exact child revalidation; F3's oneDOM and oneSQLite case separately prove unchanged uncertain-capture payload/idempotency; F4's six real Engine boundary cases admit only required source routes. TypeScript, focused Ruff/mypy and eight generated-contract hashes passed. Existing Starlette/AnyIO and RTK warnings remain visible. No full suite was rerun merely by the reviewer/controller.

Clean exact24bc78c source built through `rtk npm run build:plugin`, with before/after revision/cleanliness checks in `.local/task03/reviewed-package-20260906T024850Z/receipt.json`. XPI110097bytes/SHA256 `c1cffaaf20cbc26ee002001721160d8c22d7c7eb099185747e10463c8fb15423`; prior distribution retained under that run. Fresh host observation02:50:19Z approved the existing CPU PyInstaller path. `.local/build-reviewed-task3-engine.py` archived exact reviewed24bc78c and built in26.55seconds, exit0,214files/67593760bytes. Build receipt/log under `.local/native-smoke/m1-engine-20260906T025147Z/`; manifest SHA256 `90b791c28557bc46fbaf62317df9b863cef1ae6c343dfbba9d4d53e8c72a953a`.

`rtk proxy <existing-engine-venv-python> .local/native-smoke/check-m1-packaged-engine.py <payload> <reviewed-source>` passed in `smoke-81cfc45d97d4/smoke-receipt.json`: real process39800, authenticated notebook/idempotency, initial snapshot/history/identities/sources, source_contents migration, consumed private handshake, normal heartbeat-expiry exit0, removed receipt, closed55346socket and SQLite integrity. Child PATH was only Windows System32. This is a local frozen-runtime check, not the final release or a clean-Windows-host claim.

### Actual native Sources capture, stale version and revocation

Personal helper start/status returned ready10.0.1/API200/Connector200; fresh no-owner observation and prior Reader close/artifact hashes passed before staging. Test-only probe0.1.16/SHA256 `e88d48ecb43d360a74c95eab61a0486bd141c02be48a35dbac295c4960f7f7e7` loaded reviewed24bc78c/XPIc1cffaaf with the M1 engine. Native main40092 started02:54:41Z. Fixed synthetic Alpha395QEHY7 and BetaDNKUT3N6 were selected, while OutsideLJX2CSRI and the pre-existing Reader fixture were excluded. Trusted Enter created notebookf2f870e3; opt-ins defaulted off, preview and immutable capture contained exactly2sources, and changing Beta's native title invalidated then revalidated the original snapshot with `Versão alterada` and its original title.

`task3-native-sources-receipt.json` retains14passed checks but ends **FAILED** with `NATIVE_WAIT_TIMEOUT: historical snapshot applies current revocation`. Its full native UI and console are preserved. The UI had successfully advanced notebook revision to3 and displayed the revoke-success notice, with source details cleared. Diagnosis: the disposable helper awaited only aria-busy=false immediately after click, which could still be the previous React render; its next read encountered the product's synchronous actionBusy guard while revocation was in flight. The failed receipt is not relabeled success. Engine38748 belonged to main40092; normal close02:57:20Z succeeded and later observation found no matching engine.

A separate helper-only continuation0.1.17/SHA256 `3d5798569727b1042489fdede9300681ed1ea9b9609e4a2f47ca7751bbe442f8` retained the product/engine unchanged. After fresh helper/owner/hash/normal-close gates, main9016 started02:59:23Z. `task3-native-continuation-receipt.json` is **PASS_NATIVE_SOURCE_REVOKE_RESTART_TRASH**,11checks completed02:59:38Z. The same historical snapshot after restart displayed1accessible/1unavailable, Beta's original title marked stale, and no Alpha. Exact native Beta identity was then trashed; notification cleared metadata, an explicitly ready read displayed0accessible/2unavailable, and neither source reappeared. Main9016 closed normally02:59:49Z; engine34088 was absent02:59:50Z. No production fix or permanent test was added for the helper timing error.

The actual software-rendered900x528PNG `task3-native-sources.png` was inspected: connected engine, selected notebook, Sources panel and capture notice are legible. It shows the upper viewport only; source rows continue below it. Native group-library/attachment permutations, full accessibility/localization, disable/uninstall and later parser/retrieval/model/MCP/export consumers remain separate pending checks.

### Actual native plural collections, search and filters

The first run, `task3-native-collections-receipt.json`, **FAILED** after4checks with `NATIVE_WAIT_TIMEOUT: native plural collection selection settled`. Its predicate read nonexistent itemsView.initialized; installed itemTree.js instead exposes waitForLoad and stores internal _initialized. No Evidra workspace/engine started in that failed run. The before-close observer's expected-one-engine assertion also failed with matching:[] and is retained as such. Main33956 closed normally03:15:22Z. The synthetic collection/item identities are recorded in the failure receipt and were reused, not recreated.

After fresh personal-helper and no-owner checks, `prepare-task3-collections-corrected.py` verified product/engine/prior-close hashes, built test-only probe0.1.19 and passed node --check. Probe SHA256 `a025fef745c4ead2e82f4ee35fce6aef03bce19e1c55cb92b55b768db869e2da`; prior failure SHA256 `566126203a83e8b587bfd14fd20b3d5a27f9c23bb657ba590546955a74ec62b2`. Product24bc78c/XPIc1cffaaf and M1 remained unchanged. `task3-native-collections-corrected-receipt.json` is **PASS_NATIVE_PLURAL_COLLECTIONS_SEARCH_FILTERS**,17checks at03:20:38Z. Actual plural getters selected C1+C2 then S1, with no selected individual items or library root. Exact previews contained3deduplicated members,4with saved-search result,5with descendant opt-in,2with inclusive2024filter,1with AND tags and0after explicit Shared exclusion. Trusted Gecko input populated filter fields. Notebook641d268e captured revision2 with0accessible/0unavailable, without broadening to Outside. The receipt confirms typeof initialized=undefined and successful public waitForLoad.

The launch receipt pins20716->12972. The before-close observer verified exact M1 engine17324 parent12972; normal close succeeded03:21:02Z and the after-close observation found no owned engine03:21:03Z. No production change or new permanent test was required for this helper correction. Native same-title cross-library identities, grouped headers and attachment-specific permutations remain explicitly unverified.

### Reader loaded-byte capability and incomplete final receipt

Static installed-source receipts `task4-reader-navigation-source.{json,md}` and `task4-reader-loaded-bytes-source.{json,md}` establish the native PDF-coordinate position contract,2000ms transient highlight and complete loaded-byte API. This supports the Task4 ruling: compare actual PDFDocumentProxy bytes to the immutable SHA256 before applying a location, since Reader.open can reuse a cached document and its file URL does not prove loaded identity.

Actual capability probe0.1.20/SHA25611c91dba9a1589a115c0c9a633af6cc8a08ceac9bba33665fabd9d6284e1d660 used unchanged reviewed24bc78c/XPIc1cffaaf and existing synthetic PDFMDP6XFCU. `task4-native-reader-bytes-receipt.json` **FAILED** after6checkpoints: loaded1627bytes matched SHA2560694d022, but the optional page-text check raised TypeError: page.getTextContent is not a function. Installed pdf.mjs16059 supplies that method; explicit waiver of the content object returned by await getPage corrected the boundary. Probe0.1.21/SHA256e88c8366677de3cc8a6da9d7501f38f0bbfc2a67e930a468dddde52e8a294588 then recorded10passed checkpoints, including original_type=undefined/waived_type=function, the exact second-page sentence, region[[48,720,530,752]] on the same view/proxy, clearing after2seconds and no new annotation. The region is synthetic, not an Evidra-extracted quote rectangle.

The corrected receipt remained RUNNING after its Reader-close phase and no terminal receipt was observed. Its cause is unavailable from preserved logs. A live content-owned object becoming invalid during serialization is an unconfirmed hypothesis, not a diagnosed exception. Both native hosts closed normally (34728at03:41:14Z;29704at03:44:27Z); after-close observations found no M1 engine. `task4-reader-bytes-controller-summary.json` is **INCOMPLETE_NATIVE_FINAL_RECEIPT**, pins both original hashes and does not relabel either run. No Task4 production, cached-file replacement or complete native-run acceptance follows from these capability checkpoints. No permanent test or product change was added by root.

### Task4 M2 source review and packaged runtime

Implementation7c576209 adds registered files, bounded Windows PDFium workers, immutable text/geometry/FTS, private native text staging and actual document/evidence controls. Its original complete runs recorded49Python passes/1symlink skip/1existing warning,34TypeScript passes, clean types/lint and generated contracts. The skipped file-symlink setup failed with WinError1314; the distinct real NTFS junction refusal passed. `.superpowers/sdd/IMPLEMENTATION_PLAN/task-4-report.md` and `.local/task4/validation-receipt.json` preserve the full commands, outputs and material-invariant mapping.

Round1 at0895c86c fixes F1 pending-request ownership across parent failures, F2 PAGE_LIMIT preview classification, F3 exact-content currentness and F4 native annotation formatting. It adds three checks to existing files and extends one existing real preview case. Round2 at936da523 fixes F5 by permitting only local `data:` images under the existing CSP; two existing preview/sanitization checks pass. Round3 at2573a646 fixes F6 benign Reader bookkeeping without weakening material-change invalidation:31cases in two parameterized definitions plus15existing cases pass, with typecheck/whitespace exit0. Native lastProcessed is acknowledged only across the existing successful immutable-file verification with stable identity/path/epoch/other fields. Original RED and fixture-correction failures remain in the report. This round's initial commands lacked the required RTK prefix; the process deviation is recorded rather than concealed by repeating tests.

The final scoped review covers936da523..0c1b8cfb, including root reference documentation; F1–F6 are addressed with no new Critical/Important breakage. Root checked six source hashes, six committed blobs, ten complete original logs and five installed native-source hashes against the final receipt SHA256741cefaa08e6bd91be64e825340ebe5b87c1200469d82e619169d205c7406043. The independent final verdict is `task-4-rereview3.md`, SHA256f2ad326b3c84d25460d7fa74c3dc02bba9f00e04c3e67682ee0a4e676f4652f9. Deferred catalog diagnostics and existing verification warnings remain for final review.

The clean reviewed0c1b8cfb source built the132223-byte XPI SHA2565b63b6dd061a8f03d89e9b315242b5ed49a5d01383dddaa5f03a348cf3afdabf in `.local/task04/reviewed-package-20260906T064317Z/`. Only `content/native.js` changed from the prior reviewed CSP-corrected artifact. Its engine source tree is identical to reviewed4f75da44; the existing verified M2 frozen engine remains229files/87266700bytes, manifest SHA256788bc47ea4e2d6021e4a8c04c74f7278c934f4f7c392b892d8f56f7211bed5f4. The earlier frozen runtime failed because PyInstaller omitted installed win32timezone; the corrected build explicitly includes that existing import. The real frozen smoke then passed two-page parsing, FTS/evidence, PNG and heartbeat/socket/receipt cleanup. Its original failure and corrected runtime receipts remain under `.local/native-smoke/m2-engine-20260906T052652Z/` and `m2-engine-20260906T052849Z/`. This is not a clean-Windows-host or final-release claim.

### Actual M2 native evidence and refusal checks

The summary `.local/native-smoke/task4-native-controller-summary.json` is15211bytes, SHA2564684d2e1f7996b477755216f7d6290b447cc6215b50ceaa4254f3c3980e1b651. It pins all three passing runs, exact artifacts, original helper failures, images and normal shutdown receipts. Each used only the authorized isolated profile, synthetic data and unchanged reviewed product/engine.

| Native receipt | Executed result |
|---|---|
| `task4-source-fixed-core-receipt.json` |23checkpoints: exact child PDF scope excludes its sibling;1054bytes and2/2pages; literal FTS excerpt; actual Reader page2/label7/rotation90/crop[20,30,380,470]; full loaded SHA256 and24temporary rectangles; no annotation write; decoded50x200/2155-byte local PNG; same source/evidence panel retained after an actual empty-changed Reader notification and a complete Sources polling interval.|
| `task4-page-annotation-receipt.json` |20checkpoints:1551-byte one-page fixture has explicit page precision, exact loaded bytes and no extra rectangle/annotation. An explicitly opted-in native annotation retains the expected86characters through production staging, indexing, search and evidence. A subsequent real annotation change invalidates and detaches the document continuation.|
| `task4-replacement-fresh-receipt.json` |20checkpoints: changing one equal-length ASCII literal produces DOCUMENT_STALE refusal, explicit reindex processes2/2pages, current FTS excludes the obsolete text and returns the new literal, and actual product open refuses the still-old cached Reader with READER_VERSION_MISMATCH. Same view/proxy stays at page1 without highlight, although the new evidence targets page2.|

Original PDF SHA256b44bbbf89d63a2559da7eb7df0053b941bf4fc7fbbe3a972db2037eb3a3a21e3 and replacement SHA256cfe2bd9c5014f871b066556f098b13cedab4d87bbdef23ce9db2420b05777a77 both describe1054bytes; the successful cached refusal therefore exercises complete byte comparison. Synthetic PDFsYHD8LJR6 and3Z2I77ZC retain their deliberately changed bytes, with separate original/changed backups; no restoration is hidden. A read-only diagnostic of the owned Evidra database after the earlier helper stopped confirms both immutable versions and texts were retained. It is explicitly not native UI acceptance and never reads Zotero's database.

Root inspected the actual900x528Evidence and preview viewport captures, plus the exact region PNG. The evidence capture shows readable source/excerpt/page/label/open control without overlap. The preview shows the rotated text, local destination and full hash caption. Image SHA256 values are2361c4605aad1fed836d7a4a45dc8842d8dddaaa9fa092168032d5757e838704,923d0a656e1beb4190e8ca8004880150f1a74dc81b6edf3b0724fa76f35753d1 andb50a1a8bbbe733507e23f3b5668843a8b79de0794abcf243209ae1aa9293c067.

Native navigation diagnostics isolated hidden-host smooth animation: loaded geometry/pages remained stationary under smooth scrolling, while the same view moved with instant navigation. Product positioning checks temporarily used `general.smoothScroll=false` only in this test profile and restored both value and user-pref presence. Default animation in a visible host remains unverified. Direct page0 navigation in the replacement helper establishes a distinct cached baseline; it is not counted as product navigation. Original failures remain original: the early preview hit CSP, an early source assertion used the wrong visible key, probe0.1.32 passed an options object across the wrong realm before any file replacement, and probe0.1.33 read the previous React alert immediately after clicking reindex. Its failure UI already showed the new pending operation, and the read-only engine receipt confirms completion. The corrected fresh-fixture run0.1.34 waits for acknowledgment and passes.

All exact native hosts and engines closed normally: core28800/27920 at06:44:35–36Z, page/annotation6708/38264 at06:46:04–05Z, failed replacement23912/2160 at06:54:20–21Z, failed continuation13560/33664 at06:58:49Z and final35488/27772 at07:01:49–50Z. Native historical-ID retrieval, default visible animation, complete accessibility/localization/picker/disable/uninstall, blocked-network operation, corpus performance and later provider/MCP/export consumers remain separate final gates. The document row currently shows the latest operation's counts, including a one-page preview over a two-page PDF; final UI review must assess the missing operation-kind distinction without confusing it with incomplete parsing.

## M3 Task5 provider review and executed local adapter check

Source `12b6aa71c2c8313c22ad5d38dbf6e12cdef98993` completes six explicit adapters, profiles/capabilities, consent, secret lifecycle and usage/budgets. Independent review corrected nested iterator cleanup, causal cancellation failures, native-schema structure and malformed protocol-frame classification at b260c52. Its final covering run passed134cases, Ruff and strict mypy45files. N1 then exposed cancellation during awaited HTTP cleanup still permitting final delivery. The two-line production fix at12b6aa7 extends the existing HTTP-response/real-SQLite test: genuine RED (`DID NOT RAISE EvidraError`), then1focused GREEN proving no final, completed cleanup and CONFIRMED8/4usage with CANCELLED. Changed-file Ruff and mypy passed. The existing AnyIO warning remains; no134-case repeat was needed. Original commands/outputs and predecessor reports remain in `.superpowers/sdd/IMPLEMENTATION_PLAN/task-5-report.md` and `.local/task05/`; incremental manifest SHA2569bde34c7fd59362597adaa3e7702756fd4107039dfced068f2620c3d1c46ba9a. Scoped re-review closed all I1–I4/N1, with no new breakage. Historical initial TDD fixture/capture deviations are not relabeled as successful RED evidence.

At12:24:45UTC, `.local/task05-controller/live-adapter-smoke.py` passed through the exact archived source, FastAPI configuration, real synthetic SQLite/ScopeContext and actual Ollama HTTP generation/embedding. It made one catalog GET, one chat POST and one embedding POST. Generation returned `{ "value": 87.5, "unit": "%", "evidence_id": "SYN-1" }` in33events with61input/31output tokens confirmed; unknown monetary cost remains null. Embeddings returned3x1024 finite normalized vectors; both exact pinned models reported size_vram==size. Settings: qwen3:4b, context4096, cap128, temperature0, seed7, think=false; qwen3-embedding:0.6b uses truncate=false and full dimensions. No preload, failed-model retry, repair, provider substitution or model download occurred. First delta5.359s, generation complete5.672s, embedding2.281s, total8.797s: one synthetic smoke, not a performance benchmark.

PASS receipt: `.local/task05-controller/live-12b6aa7-corrected-20260906T1225Z/receipt.json`, SHA2560cda3122c32a6295a6f3626acde810c0d4ce0733a3c8fa470e190408f64446ff. Archive manifest SHA256c325154311dfe0e1e12b381eb69b7163de90900246779e43776ef9fe82ef04d8; reviewed source gate SHA256b2f4fedba97f1a8bc14a86a3c70512023dc10fecd52698f32b083c9aad4bec75.

Two original failures remain explicit. Readiness at12:22:18found the old owned server absent; routine authorized startup launched the same verified portable installation hidden as PID35244, preserving old owner/logs under `.local/ollama/restart-20260906T122331Z/`. Fresh12:23:47readiness verified its path/start/hash, sole loopback listener, exact model digests, no loaded jobs and available CUDA GPU. The first adapter helper failed before any provider request: HTTP profile configuration rejected forged PROBED provenance with422`Capability evidence must be server-owned.` The helper now correctly uses USER_DECLARED for its explicit synthetic capabilities; the failed receipt/traceback/executed helper remain in `live-12b6aa7-20260906T1224Z`. No product gate was bypassed and no failed model call was resent.

Limitations: no native conversation/SSE, semantic quality, vision-model, actual Windows keyring, LM Studio/paid-provider or external-network-disabled acceptance is claimed. The first security review's minor failed-keyring test used a store without the services database and inspected an unrelated backup, so that branch does not establish failed-keyring persistence safety. Earlier passing checks and current live synthetic results do not prove arbitrary-input context/truncation guarantees. Task6 owns full input-category and token-budget composition plus native consumers; later final gates remain open.

## M3 Task6 conversation source and bounded native acceptance

Task6 implements scoped SQLite vector generations, exact NumPy cosine retrieval and lexical/vector fusion, complete prepared context, explicit provider selection/consent, authenticated draft streaming, validated finals, history and cancellation. Independent review fixed historical excerpt-consent categories, cancellation on conversation unmount and Stop during a pending Start. The reviewed run dependency bridge revalidates complete authorized contents of dependent sources while registering only exact dependent PDFs; cancellation bypasses that content queue with a status-only result. Original covering backend13/TS47 and later focused consent/lifecycle checks are retained in `.superpowers/sdd/IMPLEMENTATION_PLAN/task-6-report.md` and `.local/task06/`, including original failures. One existing Starlette/AnyIO deprecation remains.

Actual native generation initially failed at reviewed d539dd1: native schema bounds were rejected by the adapter subset, and fallback schema instructions were hidden from the exact preview. The estimator already included970schema bytes; only a40-byte prefix was omitted. That estimate difference is not established as the cause of the failed generation. Source5624d33 adds four independently verified Ollama bounds, a persisted exact schema/system/mode plan, and terminal usage/reason retention. Focused consumer/context8, adapter6, UI1, historical-record1 and cleanup2 checks passed, with type/lint/build checks. The next actual native call returned complete JSON and correct literal evidence, but marked a sourced claim `general`; the unchanged domain validator refused it. Its provider usage was CONFIRMED616/150 while the domain run remained FAILED with null output. No failed output was repaired or promoted.

Source9bd75f45a9adfff605af044a2a021b446433e302 explicitly states the existing source/general/visual rules and records `conversation-v2` for new preparations. Old runs retain their original prompts/versions. Two existing exact preparation/transmitted-plan scenarios passed, and the same reviewer approved the bounded prompt/version diff. The Answer schema and evidence validator were not weakened. Root verified169 final manifest/input/artifact/receipt hashes. Final XPI is `.local/task06/artifacts/20260906T142937487640Z-fix3/evidra-0.1.0.xpi`, SHA256043ca8b25167edf7baf776917f81c5f781c971212b5d6306d550de6c699b90e5; its bytes match round2 because only backend prompt delivery changed. Final frozen engine manifest is e904218418dc873d44af24b8429fc16244a1a1a2ae615b5419ab96f727086f8f, built from the exact reviewed source using Python3.12.11/PyInstaller6.22.2 into `.local/native-smoke/m3-fix3-engine-20260906T143448Z/`.

Actual native COMPLETE runb41190ee6124e4dbdbca891be632208d used the same qwen3:4b/qwen3-embedding:0.6b digests,4096context/512output/temperature0/seed7/think=false, exact Cedar-only snapshot and existing vector generation. Its validated claim reports87.5%, kind source, real evidence ID09b2895e89c8c703f3026dcd29e742252f455c9bc00da63fdaa74b984dc164a6 and the exact original excerpt. All150events and the final remain in SQLite; provider usage is CONFIRMED647input/150output, unknown monetary cost null. The displayed3001input estimate is still explicitly not a tokenizer count or an overflow guarantee. This is one synthetic functional smoke, not a semantic-quality/performance benchmark.

The first successful-generation helper timed out during history replay because it clicked a run while the preceding history action still disabled its button. The corrected helper waited for an enabled run and reopened that exact persisted COMPLETE result after restart, without regeneration. It inspected the citation, preserved proposed-support versus verified-anchor versus human-review distinctions, opened original PDF973BSZJ5 at physical page1 and verified all1182original bytes. A separate explicit runcd497971ff0d2710b3686ebb1d08cd2a cancelled with one cancellation event and null final; its interrupted provider call remains BILLING_UNKNOWN with null counts/cost, not invented zero usage. Native Tab reached the context field; both locales rendered in the296×367Reader with no horizontal page overflow. Unsupported vision remained disabled. Test-only smooth-scroll changes were restored.

Controller inspected the actual captured views. Text, citations and localized narrow labels are legible, but the sticky status header consumes substantial space and overlaps top content in scrolled captures. Full narrow workflow/accessibility and final UI review remain open; the screenshot subset is not a complete design acceptance. Earlier helper form-readiness, notebook-identity and latest-preview-counter failures remain preserved, and no model call was repeated to correct a helper failure.

Final bounded native receipt: `.local/native-smoke/task6-native-history-receipt.json`; complete run/usage verification: `task6-native-final-run-diagnostic.json`; controller summary SHA25621ac1934878fd56751466813c2c466deeb4d2489594855b7d67fa4c96e26cfcb in the same directory binds28artifacts and preceding failure summaries. Host31220 closed normally at14:40:11Z, its exact engine was absent at14:40:12Z. All work used the existing authorized isolated profile. Real vision, Windows keyring, paid/LM Studio, offline-network, corpus-scale and full A36 matrix/evidence/export acceptance remain separate later gates.

## M4 Task7 forms and human-reviewed matrix

Source commits0ed8a31/2dd30d2/0cd2d52 and fixeed17a6 implement six field types, the15-field computing template, immutable form versions with shared unchanged-field lineage, explicit unprocessed/null value states, human proposals with current evidence, competing decisions and concrete bulk preview. Retained verification reports14matrix Python cases,1migration case,6controlled-conversation scenarios (one with visual provenance assertions),46Vitest cases, scoped mypy/Ruff and TypeScript. The original manifest binds38source blobs and24evidence files, independently verified by the controller. No controlled test is described as a real model run.

Independent review reproduced one important UI defect: editing42% to43% then reselecting the same proposal displayed43% but submitted42%. The minimal guard preserves the draft for the same ID and initializes a different ID normally. One added regression first failed at the outgoing payload assertion, then the four-case matrix UI suite passed. Final TypeScript passed after explicitly converting an absent presentation unit to null; the original failed typecheck log remains. Units now reach bulk/history displays. The fix manifest binds3source blobs and7evidence files including retained baseline identities, independently verified. No other suite was rerun for the presentation-only adjustment. The upstream Starlette/AnyIO warning remains recorded.

Final source review `task-7-rereview1.md` SHA256ddeae35e365f360860146a84fe4a93b6ae505cae54c84e7037d11d99ac95603b approved the exact candidate `eed17a602607145edd59c105a8fb67e6645f18ac`. Unique XPI `.local/task07/artifacts/20260906T160240736849Z/evidra-0.1.0.xpi` SHA2561f655523a9c30a0e8e000b90bf0a782941914736668f76534f66c5a18f47ed34 built successfully. The exact reviewed archive produced the existing CPU PyInstaller onedir engine in26.46seconds,281payload files/93377041bytes; manifest SHA256f67f14ae7e13ac9c0f0d709a955df1085c97f72970ee51f6cb004214164e1d8c. Hardware/ownership and manifest inventory were checked before native use.

Actual native checks in the existing synthetic Cedar notebook passed manual form creation, real evidence selection/PDF opening, accepted87.5% preservation after rejecting an88% competitor, concrete two-cell bulk approval, unchanged-field inheritance after one definition change, filtering, keyboard and both narrow locales. Separate restart reopened the same accepted values/proposals/history and old explicit null state with historical writes disabled. The27first-phase and18restart observations include bootstrap checks; they are not45independent domain tests. Both hosts closed normally and both engines exited; all scoped preferences were restored. The launch helper's initial missing Get-FileHash cmdlet failure happened before process creation and is retained with its equivalent .NET SHA256 correction. No product/native assertion failed and no model call or direct database mutation occurred.

Controller viewed all nine captured viewports. Values, explicit states, original excerpt and contexts are legible and the prior sticky-header obstruction is absent in the captured matrix views. At296×367, row labels truncate the repeated study prefix before the field label, and pagination displays0–3 /3; retain these minor presentation fixes for final UI acceptance. Viewports show portions of scrollable content; offscreen bulk/history details are verified by actual UI assertions. This is not full accessibility/theme/scale acceptance.

`.local/native-smoke/task7-native-controller-summary.json` SHA2560529c81c966057d8c964c1d0240644d4d0ef3eea2042c946fcb9ffced04cfd0c binds32artifacts, including both teardown chains, exact package/runtime receipts and the preserved helper failure. This completes the bounded Task7 manual-matrix slice. Model-produced extraction, measured full coverage, durable jobs, MCP/export/A36 and remaining distribution/benchmark/real-provider gates remain open.

## M4 Task8 persistent extraction source gate

Reviewed source50431ef implements paused exact preparation, scoped study/field units, unique renewable leases, persisted batch checkpoints/call IDs, explicit cancellation/resume/reconciliation and measured attachment/page/chunk coverage. Model results remain competing proposals; human values/decisions are preserved. Search misses remain distinct from complete-scan absence. Contextual experimental results retain their measurements instead of collapsing distinct datasets. Migration008 is frozen for native use.

The original controlled jobs/matrix/conversation/provider suite passed75 cases before edge corrections; affected jobs/matrix26 and later aggregation13 plus final pair2 passed. These counts overlap. Controlled UI/bridge44 cases passed; the final pending-coverage label case and TypeScript passed. Strict mypy/Ruff passed on affected code. Real SQLite/HTTP and a child-process exit after SENT establish controlled restart/authorization behavior; they do not establish native or paid-provider behavior.

Independent review found two correctness defects: metadata-only studies shared a cache key, and equivalent numeric spellings became conflicts. Fixescf17ced/3280207 bind cache identity and validate its exact target, then compare normalized numeric values in the same field/unit/context while retaining originals and citations in durable outputs. One cache case and five aggregation cases passed after their original failures; strict mypy/Ruff passed. No unchanged UI/contract/build checks were repeated. The scoped rereview approved both fixes with no new Critical/Important issue; report SHA25603d952966a16ed4b63eff6db37e3ff2ee3df60fbf5742bcf76d6940a60306d3e.

Controller verified87 initial evidence entries/41 source blobs, followed by16 round1 entries/3 changed blobs, with zero mismatches. The original report is preserved byte-for-byte. Round1 manifest `.local/task08/evidence-manifest-round1.json` SHA256c200ecece3727d0cc140c3478ef43b51763383d84341585799f5c6ca60fa6840 binds reports, original failures, focused passes and unchanged artifact identity. Existing Starlette/AnyIO deprecation and PowerShell's normal-stderr classification remain recorded, not suppressed or misreported as runtime failures.

The verified XPI `.local/task08/dist-2026-09-06T17-06-12-704Z/evidra-0.1.0.xpi` retains SHA256b651a35df00c2f2f45a02c5ea0d4cc3563c8d227fac7596a73e013ff9a4130dc; exact Git input comparison permits reuse after the backend-only fix. The reviewed50431ef archive produced the CPU PyInstaller engine `.local/native-smoke/m4-task8-engine-20260906T173139Z/` in39.61seconds,282payload files/93424935bytes, manifest SHA256f8a2549da9c6e43edbfd2dad35ebdf751036993ae711f520897f8a502fa41014. Hardware/ownership and full payload hashes were checked. Native preparation in the same authorized synthetic profile passed22 observations, including paused preparation, two selected pages and no recorded unit calls. The two prompts and field-specific schemas were inspected before inference; their input estimates2779/2590 plus512 output fit the4096 context setting. Job0de720c960397d458003595377701a93 was paused at this preparation checkpoint. Both captured viewports were inspected. Host7752 closed normally at17:33:45Z, the exact engine was absent afterward, and smooth scrolling was restored. Receipt: `.local/native-smoke/task8-native-prepare-receipt.json`. Subsequent native failures are recorded below.

## M4 Task8 native failures and cancellation correction

The original prepared job was explicitly resumed and both calls ended `GENERATION_INCOMPLETE`: confirmed745/685 input tokens and512 output tokens each, with unknown monetary cost remaining null. Current owned Ollama logs show no prompt truncation. Zero extraction results/proposals were committed. Read-only diagnostic `.local/native-smoke/task8-incomplete-diagnostic.json` preserves exact calls, unit states and causal log excerpts.

After inspecting this saturation, one distinct job0a6190c105182a82634ce50e4c2e19f8 was explicitly configured with1024 output tokens per batch; context4096, seed7, temperature0, thinkfalse, pinned model and exact two prompts/schemas remained unchanged. Its preparation helper initially read stale UI readiness and failed before any call. A separate continuation reopened that exact paused job, verified new production reads and both unchanged previews, then resumed once. Both calls again ended `GENERATION_INCOMPLETE` at1024 output tokens, with the same745/685 inputs and zero committed results. The original512 job was never retried or changed. Raw partial model text was not persisted, so its content cannot establish a more specific cause. No further output escalation or automatic retry is planned. The original helper failure and continuation failure remain separate receipts; the second read-only diagnostic is `task8-extended-incomplete-diagnostic.json`.

An independent planned cancellation test exposed a product concurrency defect. Resume returned `QUEUED` revision1; the worker claim advanced that revision before an immediate native cancel using the just-displayed revision1. Cancel returned `REVISION_CONFLICT`, and the worker dispatched a second batch. Actual calls2 therefore exceeded the intended cancellation-test maximum1. Both calls failed at512 output tokens and produced no proposals. `.local/native-smoke/task8-cancel-conflict-diagnostic.json` records this failed bound and the actual request/response sequence. Host31848 closed normally at17:58:07Z and its exact engine was absent17:58:08Z. All five Task8 native phases restored the scoped preference and ended with normal owned-host closure and exact-engine absence.

Source correction810a564570fce68a6a4028404d3ddf854e20c684 accepts older displayed revisions for restrictive pause/cancel only. Future revisions, stale resume/reconciliation, current scope, idempotency and terminal guards remain enforced. The existing controlled HTTP/SQLite inflight regression was strengthened rather than duplicated: RED2failed/1passed, then final3passed with one request, no second batch and no trusted output. The first corrected run's pause assertion incorrectly expected PAUSED; tracing existing uncertainty handling established WAITING_PROVIDER/BILLING_UNKNOWN, and only that assertion was corrected. Both original failures remain in `.local/task08/cancel-fix/`. The RED shell wrapper returned0 after displaying failing pytest output; it is explicitly not passing evidence. Final child receipt records returncode0. Changed-file Ruff and strict mypy passed; test formatting was normalized and the exact diff inspected. Scoped independent review approved the source fix, report SHA256fd875b09808bb954a673e75d479f0d4b6a2a83456807b471749e15755a766b66. This source correction has not yet been packaged or validated natively.

The workflow harness assembled `.local/native-smoke/workflow-runs/task8-native-controller-summary-88d428d045a14b6aae3c648147b4ff72/artifacts/task8-native-controller-summary.json`,21553bytes, SHA256b54478ad01c9d24f944385d7ae69319e431187e18debb8fa748c293a582c1fc6. It binds50 preserved artifacts and explicitly reports failed native extraction/cancellation acceptance. Its adjacent harness receipt means evidence assembly completed, not that product acceptance passed. The first recipe was rejected before execution because its output path omitted the required `artifacts/` prefix; the corrected recipe and original are both retained. Successful native extraction, corrected native cancellation/restart, paid-service and target-scale acceptance remain open.

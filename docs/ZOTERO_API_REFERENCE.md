# Zotero target API reference

Target inspected: Zotero 10.0.1 / Firefox 140 ESR, 2026-09-05. This is a source reference, not a claim that the Evidra integration has passed native smoke.

| Boundary | Target API | Source and implementation constraint |
|---|---|---|
| Bootstrap | install/startup/onMainWindowLoad/onMainWindowUnload/shutdown/uninstall | [Official sample](https://raw.githubusercontent.com/zotero/make-it-red/master/src-2.0/bootstrap.js). Remove window listeners/UI and owned processes on unload. |
| Menus | Zotero.MenuManager.registerMenu / unregisterMenu | [Target source](https://raw.githubusercontent.com/zotero/zotero/10.0.1/chrome/content/zotero/xpcom/pluginAPI/menuManager.js). Use menuID/pluginID/target/menus and plural context.collectionTreeRows. |
| Workspace | Native DOM dialog/panel in Zotero main window | [Target tabs source](https://raw.githubusercontent.com/zotero/zotero/10.0.1/chrome/content/zotero/tabs.js). Do not use an unknown Zotero_Tabs.add type: getState persists it, restoreState requires a defined hook and can fail after removal. ADR 0002 selects a panel and leaves host session methods untouched. |
| Reader-adjacent panel | Zotero.ItemPaneManager.registerSection | [Target source](https://raw.githubusercontent.com/zotero/zotero/10.0.1/chrome/content/zotero/xpcom/pluginAPI/itemPaneManager.js). paneID/pluginID/header/sidenav/bodyXHTML and lifecycle/render hooks; do not invent a separate reader sidebar API. |
| Selection | getCollectionTreeRows/getSelectedLibraryIDs/getSelectedCollections/getSelectedSavedSearches | [Zotero 10 changes](https://www.zotero.org/support/dev/zotero_10_for_developers). Singular selection getters throw; mixed collections/searches/libraries allowed. |
| Item list | getSelectedItems/getSortedItems; row.isObjectRow for manual walking | [Target view](https://raw.githubusercontent.com/zotero/zotero/10.0.1/chrome/content/zotero/collectionViewItemTree.jsx). Skip library headers/spacers. |
| Saved search | Zotero.Search / search results; item.parentID/parentKey | [Search source](https://raw.githubusercontent.com/zotero/zotero/10.0.1/chrome/content/zotero/xpcom/data/search.js). Child results need parent normalization while retaining selected content permission. |
| Library availability and write permission | Zotero.Libraries.exists/get; library.archived; editable/filesEditable; item.isEditable() | [Library registry](https://raw.githubusercontent.com/zotero/zotero/10.0.1/chrome/content/zotero/xpcom/data/libraries.js), [library source](https://raw.githubusercontent.com/zotero/zotero/10.0.1/chrome/content/zotero/xpcom/data/library.js). Availability and write permission are different checks; local IDs do not stand for remote group IDs. |
| File resolution | await attachment.getFilePathAsync() | [Item source](https://raw.githubusercontent.com/zotero/zotero/10.0.1/chrome/content/zotero/xpcom/data/item.js). Returns path or false; engine independently revalidates regular file/reparse/hash/identity. |
| Page navigation | Zotero.Reader.open(attachmentID, {pageIndex}); reader.navigate(location) | [Reader source](https://raw.githubusercontent.com/zotero/zotero/10.0.1/chrome/content/zotero/xpcom/reader.js). Physical pageIndex is zero-based; page labels are separate. |
| Annotations | attachment.getAnnotations; Zotero.Annotations.toJSON | [Annotation source](https://raw.githubusercontent.com/zotero/zotero/10.0.1/chrome/content/zotero/xpcom/annotations.js). Preserve text/comment/position/type and human provenance. |
| Approved note | new Zotero.Item('note'), setNote, saveTx, Zotero.Items.getAsync | [Native tests](https://raw.githubusercontent.com/zotero/zotero/10.0.1/test/tests/itemTest.js). Permission, preview, outbox UUID, reconcile and readback precede acknowledging note creation; creation is not undoable. |
| Change signals | Zotero.Notifier.registerObserver/unregisterObserver | [Notifier source](https://raw.githubusercontent.com/zotero/zotero/10.0.1/chrome/content/zotero/xpcom/notifier.js). Register item/collection/search/group/tab interests and remove observer on shutdown. |
| Bibliography | new Zotero.Translate.Export; setItems/getTranslators/setTranslator/setLocation/translate | [Native export wrapper](https://raw.githubusercontent.com/zotero/zotero/10.0.1/chrome/content/zotero/fileInterface.js). Use only selected permitted item objects; no mandatory Better BibTeX. |
| Owned helper | Mozilla Subprocess.call/wait/kill | [Subprocess docs](https://firefox-source-docs.mozilla.org/toolkit/modules/subprocess/toolkit_modules/subprocess/index.html). Arrays of arguments, no shell, pipe logs without secrets, kill only owned child if necessary. |
| Privileged local I/O | IOUtils.readJSON/writeJSON/writeUTF8/stat/remove/makeDirectory | [IOUtils docs](https://firefox-source-docs.mozilla.org/dom/ioutils_migration.html). Atomic temp file doesn't establish Windows DACL security by itself. |

Annotation text/comment use plain text with matched, case-insensitive `i`, `b`, `sub` and `sup` formatting pairs; they are not arbitrary HTML. Preserve literal entities, unknown tag-like text and unmatched markers. Installed `editorInstance.js:1842–1907` starts with text nodes and recognizes only those pairs; lines1791–1801 apply the formatter separately to annotation text and comment, and line1991 exposes `Zotero.EditorInstanceUtilities`. Composing `_transformTextToHTML` with the existing HTML-to-text utility preserves literals such as `List<T>` and `&amp;`; directly treating the original annotation fields as HTML loses them. The [10.0.1 source contract](https://github.com/zotero/zotero/blob/10.0.1/chrome/content/zotero/xpcom/editorInstance.js#L1755) and `.local/native-smoke/task4-annotation-contract-source.json` pin the algorithm; installed member SHA256 is `d4e54822d111dfc3be8a1842e3d01a3393f8b1257963f52a4bca943dc92c793f`. This is static API evidence; actual native annotation ingestion remains unverified.

Native Reader observation on 10.0.1 (`.local/native-smoke/task2-native-reader-receipt.json`): `Zotero.Attachments.importFromFile({file,parentItemID,title,contentType})` imported the fixed synthetic PDF into the guarded profile. `Reader.open` selected the exact attachment; `getByTabID(win.Zotero_Tabs.selectedID)` and `reader._window` established ownership. The tab-specific `item-details` id is `reader.tabID + '-context'`. `getCustomPanes()` supplied the actual namespaced paneID; `pane.open=true`, context mode=item/collapsed=false and `scrollToPane(paneID,'instant')` rendered the visible 296x367 Evidra frame. `isPaneVisible`, exact item/tab ownership and null principal were checked. Reader close removed its context; main workspace reopened successfully. This does not yet prove evidence location/quote geometry.

Installed Reader navigation source inspected on2026-09-06: `resource/reader/reader.js:76789–76814` accepts `{position:{pageIndex,rects:[[xMin,yMin,xMax,yMax],...]}}`, navigates to its bounds and highlights it transiently for2000ms (`76631–76637`). Rectangles remain in PDF coordinates: `76899–76913` applies the page viewport conversion, so callers must not pre-apply display rotation or crop translation. The position branch does not create a Zotero annotation. A plain `{pageIndex}` opens the physical zero-based page without a rectangle; page labels are handled separately. `chrome/content/zotero/xpcom/reader.js:755–756` forwards location to the inner Reader without awaiting its completion, so a resolved wrapper Promise alone is not native destination proof. Full bounded excerpts and source hashes: `.local/native-smoke/task4-reader-navigation-source.{json,md}`; resource SHA256efcae743e5de2f8a1d47587a76dc2c5a863cf3765f242585f3f187c6b6b6ddf5. This is static API evidence; Evidra's actual quote geometry remains a separate test.

Loaded-document identity requires a separate check. The installed wrapper's `_getData:677–692` returns an attachment URL, and `Reader.open:2922–2950` may reuse a loaded Reader or return undefined while activating an unloaded tab. The captured active PDF view `reader._internalReader._lastView` exposes `initializedPromise` and `_iframeWindow.PDFViewerApplication.pdfDocument`. PDFDocumentProxy.getData (`pdf.mjs:15761–15763,16817–16818`) reaches the PDF worker's complete `requestLoadedStream().then(stream=>stream.bytes)` (`pdf.worker.mjs:66375–66376`). Hash those bytes against the immutable document version before any evidence location, then recheck scope and exact view/proxy identity. `_lastView` is a getter affected by split focus/Reading Mode; missing PDF capabilities must fail explicitly. `documentloaded` follows only firstPagePromise and is insufficient byte-identity proof. Static receipts/excerpts: `.local/native-smoke/task4-reader-loaded-bytes-source.{json,md}`. The first verification necessarily loads the authorized PDF's complete bytes; native operation remains unverified.

The separate native capability probe subsequently observed complete1627-byte SHA256 matching, stable view/proxy, exact physical-page text and transient region clearing without annotation creation. Returning a PDFPageProxy through await required an explicit Cu.waiveXrays: the native diagnostic measured getTextContent as undefined before waiver and function afterward. `task4-reader-bytes-controller-summary.json` preserves these checkpoints while reporting an incomplete final receipt after Reader close; it is not a completed Task4 product test. For future diagnostics, copy content-owned arrays/objects into primitive receipt data before destroying their window.

The installed PDFDocumentProxy.getDownloadInfo (`resource/reader/pdf/build/pdf.mjs:15770–15772`) resolves the transport's DataLoaded object (`16705–16710`). The worker derives its length from complete stream.bytes.byteLength (`pdf.worker.mjs:66268–66271`). Compare this length with the immutable evidence document byte count before copying getData into the bridge, then retain the full SHA256 and current view/proxy/scope checks. This is not a memory limit on the native Reader: it has already loaded the stream before reporting that length. Bounded source excerpts/hashes are in `.local/native-smoke/task4-reader-download-info-source.{json,md}`; this particular API has not yet been observed in a native product test.

Operational policy remains separate: ordinary Zotero operations use only the personal local helper, which lacks profile-selecting startup arguments. The user explicitly authorized the exact isolated-profile native test exception in NATIVE_VALIDATION_PLAN.md; that exception does not authorize personal-profile UI automation or library mutation.

## Test-profile isolation reference

The target [dataDirectory.js](https://raw.githubusercontent.com/zotero/zotero/10.0.1/chrome/content/zotero/xpcom/dataDirectory.js) handles an explicit `-datadir` before preference/default discovery (lines 55–78); it accepts an absolute path with an existing parent, or `profile`. A profile alone is insufficient isolation: the ordinary default-discovery branch inspects other profiles and the default data directory. The test launcher therefore must supply both an isolated profile and an explicit disposable data directory, then verify the running profile/data paths before any fixture write. This is a source-derived harness requirement, not evidence of an executed isolated launch. The [official multiple-profile guide](https://www.zotero.org/support/kb/multiple_profiles) also documents that automatic separate-data-directory behavior has not always occurred.

Two ancillary raw-source lookups for `defaults/preferences/prefs.js` and `app/assets/app.ini` returned tool Internal Error on 2026-09-05. They supplied no evidence; no startup or preference operation was attempted from them.

## Reading versus editing a group

`item.isEditable()` checks editing, including imported-file editing; it is not a read-permission API. A current group member can have `editable=false` and `filesEditable=false`. The source resolver must still support selected available content in that group, while future note writes require write permission. See [item.js](https://raw.githubusercontent.com/zotero/zotero/10.0.1/chrome/content/zotero/xpcom/data/item.js) and [groups.js](https://raw.githubusercontent.com/zotero/zotero/10.0.1/chrome/content/zotero/xpcom/data/groups.js).

The target sync runner marks a retained group `archived=true` when group access or membership disappears. Evidra's current-access requirement therefore treats an archived or missing library as unavailable, even when Zotero retains its local files. This is an implementation ruling inferred from [syncRunner.js](https://raw.githubusercontent.com/zotero/zotero/10.0.1/chrome/content/zotero/xpcom/sync/syncRunner.js), not a new remote permission check. The bridge observes Zotero's current local state; it cannot detect an unsynced remote membership change without Zotero reporting it. Revalidate known selected libraries/items on operations and revoke on relevant notifications; never call cloud APIs or scan unrelated libraries to discover changes.

The initial lookup of `xpcom/libraries.js` returned Internal Error; the verified registry is `xpcom/data/libraries.js`. The failed URL supplied no evidence and caused no Zotero operation.

## Measured Gecko transport and path behavior

The isolated native experiment at23:18Z confirms that a sandboxed content page cannot call its chrome parent's postMessage. A self-posted request can reach a privileged listener attached to that exact contentWindow: source is the child, origin is `null`, and isTrusted is true. The privileged reply arrives with source=null, empty origin and isTrusted=true. Receipt `.local/native-smoke/task2-native-transport-package-diagnostic-bounded.json` preserves both directions and the opaque principal. [MDN documents the null source for privileged postMessage](https://developer.mozilla.org/en-US/docs/Web/API/Window/postMessage#using_window.postmessage_in_extensions); the empty origin is a measured target behavior, not a portable browser assumption. A native-ready signal must precede queued UI requests, and synthetic/unexpected senders must be rejected.

The same experiment found `PathUtils.join(system, 'WindowsPowerShell\\v1.0\\powershell.exe')` throws OperationError/NS_ERROR_FILE_UNRECOGNIZED_PATH. Pass each fixed path component separately. Preserve OperationError/UnknownError and the numeric DOMException result in structural diagnostics; [Mozilla documents the IOUtils exception categories](https://firefox-source-docs.mozilla.org/dom/ioutils_migration.html#errors-are-reported-as-domexceptions). Raw native messages may contain paths and are not suitable for public error payloads.

Installed `omni.ja/modules/subprocess/subprocess_common.sys.mjs:495-526` confirms readString without a length returns an empty string at EOF and decodes successive chunks in streaming mode. This matches the adapter's pipe-drain contract; supplying an exact length has different EOF rejection behavior.

The actual production iframe delivers its child document load through capture phase, with the document as event target. Initialize only after a trusted event for the exact owned contentDocument, fixed expected UI URI and complete readiness; ignore early resource/foreign-document loads without consuming the listener. Native receipt `task2-native-load-diagnostic.json` establishes the target behavior, corroborated by [Mozilla's chrome iframe discussion](https://bugzilla.mozilla.org/show_bug.cgi?id=1657182#c5).

The UI's `sandbox="allow-scripts"` also blocks native form submission before the submit event is fired. The [HTML submission algorithm](https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#form-submission-algorithm) returns on the sandboxed-forms flag at step4, before event dispatch at step5.6. Actual receipt `task2-native-trusted-form-diagnostic.json` proves trusted input, React state update and an enabled trusted click, followed by no submit or bridge request. UI commands therefore use explicit type=button actions and appropriate single-line Enter handling, with IME/busy checks. A form element may provide semantics and styling; its submit event cannot be the command path. Keep the existing sandbox and CSP unchanged.

The earlier test helper's synthetic input event did not reach the content's normal event path. For authorized test-profile input only, Mozilla's [nsITextInputProcessor interface](https://searchfox.org/mozilla-central/source/dom/interfaces/base/nsITextInputProcessor.idl) and [Input.insertText implementation discussion](https://bugzilla.mozilla.org/show_bug.cgi?id=1595177#c6) define beginInputTransactionForTests/commitCompositionWith. Root verified the exact child document and focused input before using it; the actual receipt records trusted capture/bubble events and matching input value/React attribute. This is test instrumentation, not a production bridge capability.

The same opaque content realm reports isSecureContext=false and crypto.randomUUID=undefined, while getRandomValues is available and returns the requested32bytes. Privileged Xray inspection of that same window misleadingly reports randomUUID as a function. The fixed content-script experiment `task2-native-crypto-diagnostic.json` records the actual TypeError and both observations. [Gecko ESR140 Crypto.webidl](https://raw.githubusercontent.com/mozilla/gecko-dev/esr140/dom/webidl/Crypto.webidl) marks randomUUID as SecureContext and does not mark getRandomValues. Production UI keys therefore use getRandomValues directly, with no conditional provider or algorithm fallback.

The actual authorized multiwindow experiment `task2-native-multiwindow-receipt.json` verifies `Zotero.openMainWindow()` plus identification through the new member of `Zotero.getMainWindows()`, exact document URI `chrome://zotero/content/zoteroPane.xhtml` and ready collectionsView. Acquire each window's fresh Tools command and iframe from its own document. Closing the second native window preserves the first workspace; normal final-window close removes the owned engine. Native keyboard testing uses the focus-verified frame's TextInputProcessor keydown/keyup pair; always keyup in finally.

Within the isolated profile, `Zotero.Prefs.set('browser.theme.toolbar-theme',1,true)` propagated light mode and value0 dark mode to both actual frame media queries and CSS colors. Preserve/restore the user's prior preference presence, not only its numeric value. Setting the exact owned iframe's `docShell.browsingContext.fullZoom=2` produced450x264logical viewport and the responsive single-column layout, with no horizontal overflow; reset to its original value. These are measured target APIs for authorized tests, not newly exposed product bridge operations.

The native Reader navigation experiments are preserved in `.local/native-smoke/task4-native-navigation-controller-summary.json` (SHA256 `156b35d23ce7006aad3e2c189f69db78d5ddfbb875e84778d321e5f959972993`). In the hidden test host, default neighboring-page smooth scrolling remained stationary despite nonzero geometry and loaded pages; waiting for pagesPromise, issuing a later native call and closing the workspace did not change that. An explicit diagnostic instant-scroll call moved the same PDF to physical page2. A separate fresh run then used the unchanged production Evidence button once with `general.smoothScroll=false` only in the isolated test profile: actual page2 and its temporary rectangles passed, and the preference's original value and absence of a user override were restored in finally. Mozilla's [smooth-scroll preference test](https://searchfox.org/firefox-main/source/layout/base/tests/test_smooth_scroll_preference.html) confirms that this preference controls explicit smooth requests. This is positioning evidence with animation disabled; default animation in a visible window remains unverified. Do not add arbitrary readiness delays or diagnostic direct navigation to production.

The same first-open trace records `modify/item` with an empty `changed` object after native last-read bookkeeping, followed by Sources invalidation. Empty `changed` plus unchanged item version/dateModified cannot prove a harmless event: installed `item.js` tracks attachmentPath/linkMode/contentType outside the previous-data fields used by `dataObject.js` to construct this payload, and native relinking can preserve dateModified. Notes, annotation changes and trash must retain invalidation. A suppression decision requires an already authorized, verified unchanged observation, with unknown states and actual file/access changes still failing closed; the native event payload alone is insufficient.
